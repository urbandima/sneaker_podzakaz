# 🔧 ИСПРАВЛЕНИЕ ОШИБКИ ПОДКЛЮЧЕНИЯ К БД

**Ошибка**: `Connection refused` - не могу подключиться к базе данных

---

## 🎯 3 СПОСОБА РЕШЕНИЯ

### ✅ СПОСОБ 1: Локальная БД (БЫСТРО, 2 минуты)

**Для разработки без удаленного сервера**

#### Шаг 1: Установите MySQL (если нет)
```bash
brew install mysql
brew services start mysql
```

#### Шаг 2: Создайте базу данных
```bash
mysql -u root
```

```sql
CREATE DATABASE order_management CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'order_user'@'localhost' IDENTIFIED BY 'order_password';
GRANT ALL PRIVILEGES ON order_management.* TO 'order_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

#### Шаг 3: Настройте config/db.php
Раскомментируйте блок "РЕЖИМ 2":

```php
// В конце файла config/db.php (строки 73-79)
// Раскомментируйте:
if ($isLocal) {
    $config['dsn'] = 'mysql:host=127.0.0.1;dbname=order_management';
    $config['username'] = 'order_user';
    $config['password'] = 'order_password';
}
```

#### Шаг 4: Импортируйте схему (если есть dump)
```bash
mysql -u order_user -p order_management < database.sql
```

**Готово!** ✅

---

### ✅ СПОСОБ 2: SSH Туннель к удаленной БД

**Для работы с продакшн данными**

#### Шаг 1: Установите sshpass (если нет)
```bash
brew install sshpass
```

#### Шаг 2: Запустите туннель
```bash
chmod +x start-ssh-tunnel.sh
./start-ssh-tunnel.sh
```

#### Проверка:
```bash
./check-ssh-tunnel.sh  # Если есть
# Или
lsof -i :3306
```

**Готово!** ✅

---

### ✅ СПОСОБ 3: Временно отключить debug (БЫСТРО, 10 секунд)

**Для работы без БД (только frontend)**

#### В config/web.php закомментируйте debug:
```php
$config = [
    'id' => 'order-management',
    'name' => 'Система управления заказами',
    'bootstrap' => [
        'log',
        // 'debug',   // ← ЗАКОММЕНТИРУЙТЕ
        // 'gii',     // ← ЗАКОММЕНТИРУЙТЕ
    ],
```

**Минусы**: Не будет работать админка и сохранение данных  
**Плюсы**: Можно смотреть дизайн фронтенда ✅

---

## 🚀 РЕКОМЕНДАЦИЯ

**Для разработки**: Используйте **СПОСОБ 1** (локальная БД)
- ✅ Быстро
- ✅ Не требует интернет
- ✅ Полный контроль
- ✅ Можно создавать тестовые данные

---

## 📝 АВТОМАТИЧЕСКОЕ ИСПРАВЛЕНИЕ

Я могу автоматически настроить локальную БД:
