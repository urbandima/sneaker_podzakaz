# IDE
.idea
.vscode
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Composer
/vendor
composer.phar
composer.lock

# Runtime
/runtime/*
!/runtime/.gitkeep

# Assets
/web/assets/*
!/web/assets/.gitkeep

# Uploads (НЕ коммитим файлы клиентов)
/web/uploads/*
!/web/uploads/.gitkeep

# Config (локальные переопределения конфигов)
/config/db-local.php
/config/db-production.php
/config/params-local.php
.env

# SECURITY: Файлы с паролями и секретами
*DEPLOY*.md
!DEPLOYMENT_GUIDE.md
*COMMANDS*.md
ssh-deploy.sh
deploy.sh
DEPLOY_INFO.txt
SECURITY_AUDIT.md

# SSH туннель (временные файлы)
/tmp/ssh-tunnel-*.pid
.ssh-tunnel.pid

# Tests
phpunit.phar
/phpunit.xml

# Vagrant
/.vagrant

# Documentation (НЕ деплоим на production, оставляем только в Git)
# Эти файлы будут в репозитории, но не попадут на сервер при правильном деплое
# Используйте: rsync --exclude-from=.gitignore или git archive
