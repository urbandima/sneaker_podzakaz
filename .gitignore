# yii console commands (removed to allow deployment)
# /yii

# phpstorm project files
.idea

# netbeans project files
nbproject

# zend studio for eclipse project files
.buildpath
.project
.settings

# windows thumbnail cache
Thumbs.db

# composer vendor dir
/vendor

# composer itself is not needed
composer.phar

# Mac DS_Store Files
.DS_Store

# phpunit itself is not needed
phpunit.phar

# local phpunit config
/phpunit.xml

# vagrant runtime
/.vagrant

# ignore generated files
/web/index-test.php
/web/robots.txt

# ignore uploaded files
/web/uploads/*
!/web/uploads/.gitkeep

# ignore runtime files
/runtime/*
!/runtime/.gitkeep

# ignore log files
/web/assets/*
!/web/assets/.gitkeep

# composer lock file
composer.lock
