# Быстрый старт: Импорт Poizon в админке

## 🚀 Доступ к функционалу

После авторизации в админ-панели как администратор:

1. Открой **http://localhost:8080/admin**
2. В верхнем меню увидишь пункт **🚀 Импорт Poizon**
3. Кликни на него для доступа к дашборду импорта

## 📊 Главный дашборд (`/admin/poizon-import`)

### Статистика
- **Всего импортировано** - общее количество товаров
- **Успешных импортов** - количество завершенных батчей
- **Ошибок** - общее количество ошибок
- **Успешность** - процент успешных импортов

### Последний импорт
Показывает статус, время начала/завершения, длительность и результаты последнего запуска.

### История импортов
Таблица всех запусков импорта с:
- ID батча
- Источник (Poizon/Manual/API)
- Тип (Full/Update/Sizes)
- Статус (Pending/Processing/Completed/Failed)
- Время старта
- Длительность
- Результаты (создано/обновлено/пропущено/ошибок)

## ▶️ Запуск импорта (`/admin/poizon-run`)

1. Кликни кнопку **"Запустить импорт"** на дашборде
2. Установи лимит товаров (по умолчанию: 100)
3. Нажми **"Запустить импорт"**
4. Импорт запускается в фоновом режиме
5. Страница автоматически обновляется каждые 5 секунд во время процесса

### Настройки импорта
- **Лимит товаров**: 1-10,000 (рекомендуется 100-500 для первого запуска)
- **Режим**: фоновый процесс через `exec()`
- **Автопубликация**: все товары публикуются автоматически
- **Статус**: "Под заказ"
- **Срок доставки**: 14-30 дней

## 🔍 Детали батча (`/admin/poizon-view?id=X`)

Кликни на любой батч в истории для просмотра:

### Информация о батче
- Статус, источник, тип
- Время начала и завершения
- Длительность
- Кто запустил (пользователь или Cron)

### Статистика батча
- Создано товаров
- Обновлено товаров
- Пропущено
- Ошибок
- Процент успешности

### Конфигурация
JSON-конфигурация запуска (лимиты, параметры)

### Логи импорта
Детальная таблица каждого товара:
- Время
- Действие (Created/Updated/Skipped/Error)
- SKU
- Название товара
- Сообщение

**Auto-refresh**: Страница обновляется каждые 3 секунды, пока импорт активен.

## ⚠️ Ошибки импорта (`/admin/poizon-errors`)

Показывает только записи с ошибками:
- Время ошибки
- ID батча (кликабельный)
- SKU
- Название товара
- Сообщение об ошибке
- Детали (кнопка с JSON)

## 🔌 API Endpoints

### Статус последнего импорта
```javascript
GET /admin/poizon-status

Response:
{
  "status": "completed",
  "batch_id": 5,
  "started_at": "2024-11-04 12:00:00",
  "finished_at": "2024-11-04 12:15:30",
  "progress": {
    "total": 100,
    "created": 85,
    "updated": 10,
    "errors": 5
  },
  "success_rate": 95.0
}
```

### Проверка размера в реальном времени
```javascript
GET /admin/poizon-check-size?poizonSkuId=ABC123

Response:
{
  "success": true,
  "available": true,
  "stock": 15,
  "price_cny": 899.00,
  "delivery_days": "14-30"
}
```

## 🎯 Типичный рабочий процесс

1. **Первый запуск**
   - Заходишь в `/admin/poizon-import`
   - Кликаешь "Запустить импорт"
   - Ставишь лимит 100 товаров
   - Запускаешь

2. **Мониторинг**
   - Страница автоматически обновляется
   - Видишь прогресс в реальном времени
   - Проверяешь статистику

3. **Проверка результатов**
   - Кликаешь на батч для детального просмотра
   - Изучаешь логи
   - Если есть ошибки - переходишь в `/admin/poizon-errors`

4. **Повторный запуск**
   - Для обновления цен и размеров
   - Запускаешь импорт снова
   - Существующие товары обновляются, новые создаются

## 📝 Консольные команды (альтернатива)

Если нужно запустить импорт из терминала:

```bash
# Полный импорт
php yii poizon-import/run --limit=100

# Тестовый запуск (dry-run)
php yii poizon-import/run --limit=10 --dry-run=1

# Обновление только цен
php yii poizon-import/update-prices

# Обновление только размеров
php yii poizon-import/update-sizes

# Просмотр логов
php yii poizon-import/logs

# Просмотр логов конкретного батча
php yii poizon-import/logs --batch-id=5

# Тест подключения к API
php yii poizon-import/test
```

## ⏰ Автоматический импорт (Cron)

Настроен ежедневный автоматический импорт через cron:

```bash
# Запускается каждый день в 03:00
0 3 * * * /path/to/project/poizon-import-cron.sh >> /path/to/project/runtime/logs/cron.log 2>&1
```

Проверь настройку:
```bash
crontab -l
```

## 🔐 Права доступа

- **Только администраторы** имеют доступ к функционалу импорта
- Менеджеры и логисты не видят пункт меню
- При попытке прямого доступа - 404 ошибка

## 💡 Полезные советы

1. **Первый запуск**: начни с 100-200 товаров для теста
2. **Регулярность**: запускай импорт ежедневно для актуальности
3. **Мониторинг**: следи за процентом ошибок (норма: до 5%)
4. **Размеры**: проверяй наличие размеров через API перед оформлением заказа
5. **Логи**: периодически просматривай ошибки для выявления проблем

## 🆘 Решение проблем

### Импорт завис (статус Processing > 30 минут)
```bash
# Проверь процессы PHP
ps aux | grep "poizon-import"

# Останови зависший процесс
kill -9 PID

# Запусти заново из админки
```

### Ошибка "Class not found"
```bash
# Обнови autoload
composer dump-autoload

# Очисти кеш
rm -rf runtime/cache/*
```

### API не отвечает
```bash
# Тест подключения
php yii poizon-import/test

# Проверь параметры в config/params.php
# Проверь poizonApiKey и poizonApiUrl
```

## 📚 Дополнительная документация

- **Полное руководство**: `POIZON_INTEGRATION.md`
- **Архитектура**: `POIZON_ARCHITECTURE.md`
- **Задачи проекта**: `PROJECT_TASKS.md`

---

**Создано**: 04.11.2024  
**Версия**: 1.0  
**Автор**: Cascade AI Assistant
