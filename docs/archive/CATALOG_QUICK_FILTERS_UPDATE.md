# Обновление быстрых фильтров и заголовка каталога

## Дата: 2025-11-05 14:28

## Выполненные улучшения

### 1. ✅ Компактный заголовок с счетчиком в одну строку

**Было:**
```html
<div class="header-title">
    <h1>Каталог</h1>
    <p class="subtitle">Найдено товаров: <strong>42</strong></p>
</div>
```

**Стало:**
```html
<h1>Каталог <span class="products-count">(42)</span></h1>
```

**Стили:**
```css
.content-header h1 {
    display: flex;
    align-items: center;
    gap: 0.5rem;
}
.products-count {
    font-size: 0.875rem;
    font-weight: 500;
    color: #666;
}
```

**Эффект:**
- Экономия ~20px вертикального пространства
- Информация компактнее и понятнее
- Счетчик визуально не перегружает интерфейс

---

### 2. ✅ Быстрые фильтры: Бренды + Размеры

**Было:** Скидки, Новинки, В наличии, Хиты

**Стало:** 
- **Топ-6 брендов** с количеством товаров (Nike, Adidas, Puma и т.д.)
- **Разделитель** (вертикальная линия)
- **Популярные размеры** (39, 40, 41, 42, 43, 44)

**PHP код:**
```php
<!-- Топ-6 брендов -->
<?php 
$topBrands = array_slice($filters['brands'], 0, 6);
foreach ($topBrands as $brand): 
    if ($brand['count'] > 0): ?>
    <button class="quick-chip brand-chip" 
            data-brand="<?= $brand['id'] ?>" 
            onclick="toggleBrandFilter(<?= $brand['id'] ?>, '<?= $brand['slug'] ?>')">
        <span><?= $brand['name'] ?></span>
        <span class="chip-count"><?= $brand['count'] ?></span>
    </button>
<?php endif; endforeach; ?>

<div class="quick-divider"></div>

<!-- Размеры -->
<?php 
$popularSizes = ['39', '40', '41', '42', '43', '44'];
foreach ($popularSizes as $size): ?>
    <button class="quick-chip size-chip" 
            data-size="<?= $size ?>" 
            onclick="toggleSizeFilter('<?= $size ?>')">
        <i class="bi bi-rulers"></i>
        <span><?= $size ?></span>
    </button>
<?php endforeach; ?>
```

---

### 3. ✅ Новые стили для чипсов

**Brand Chips** (с счетчиком):
```css
.brand-chip .chip-count {
    font-size: 0.6875rem;
    color: #999;
    margin-left: 0.25rem;
    font-weight: 500;
}
.brand-chip:hover .chip-count { color: #fff; }
.brand-chip.active .chip-count { color: #fff; }
```

**Size Chips** (компактные):
```css
.size-chip {
    padding: 0.375rem 0.625rem;
    min-width: 48px;
    justify-content: center;
}
.size-chip i { font-size: 0.75rem; }
```

**Разделитель:**
```css
.quick-divider {
    width: 1px;
    height: 24px;
    background: #e5e7eb;
    margin: 0 0.25rem;
}
```

---

### 4. ✅ JavaScript функции фильтрации

**Фильтр по бренду:**
```javascript
function toggleBrandFilter(brandId, brandSlug) {
    const button = event.currentTarget;
    const isActive = button.classList.contains('active');
    
    button.classList.toggle('active');
    
    const checkbox = document.querySelector(
        `input[name="brands[]"][value="${brandId}"]`
    );
    
    if (checkbox) {
        checkbox.checked = !isActive;
        setTimeout(() => {
            if (typeof applyFilters === 'function') {
                applyFilters();
            }
        }, 100);
    }
}
```

**Фильтр по размеру:**
```javascript
function toggleSizeFilter(size) {
    const button = event.currentTarget;
    const isActive = button.classList.contains('active');
    
    button.classList.toggle('active');
    
    const checkbox = document.querySelector(
        `input[name="sizes[]"][value="${size}"]`
    );
    
    if (checkbox) {
        checkbox.checked = !isActive;
        setTimeout(() => {
            if (typeof applyFilters === 'function') {
                applyFilters();
            }
        }, 100);
    }
}
```

---

### 5. ✅ Адаптивность

**Mobile (<640px):**
```css
.content-header h1 { 
    font-size: 1rem; 
    flex-wrap: wrap; 
}
.products-count { font-size: 0.75rem; }
.brand-chip .chip-count { font-size: 0.625rem; }
.size-chip { 
    padding: 0.3rem 0.5rem; 
    min-width: 42px; 
}
.quick-divider { height: 20px; }
```

**Tablet (768px-1024px):**
```css
.content-header h1 { font-size: 1.125rem; }
.products-count { font-size: 0.9375rem; }
```

**Desktop (≥1024px):**
```css
.content-header h1 { font-size: 1.25rem; }
.products-count { font-size: 1rem; }
```

---

## Преимущества новой системы

### UX улучшения:
1. ✅ **Быстрый подбор по брендам** — одним кликом по топовым брендам
2. ✅ **Моментальный фильтр размеров** — без открытия сайдбара
3. ✅ **Видимость товаров** — счетчик показывает количество для каждого бренда
4. ✅ **Компактный заголовок** — не занимает много места
5. ✅ **Визуальная обратная связь** — активные чипсы выделяются черным

### Практическая польза:
- Пользователь **сразу видит топовые бренды**
- Можно **быстро фильтровать по размеру** без лишних действий
- **Экономия кликов** — не нужно открывать sidebar для простых фильтров
- **Показывается актуальная информация** — сколько товаров по каждому бренду

### Технические преимущества:
- Динамическая генерация из `$filters['brands']`
- Синхронизация с чекбоксами в sidebar
- Работает через существующую функцию `applyFilters()`
- Полная адаптивность под все устройства

---

## Визуальная структура

```
┌─────────────────────────────────────────────────────────┐
│ Каталог (42)                                            │
├─────────────────────────────────────────────────────────┤
│ [Nike 12] [Adidas 8] [Puma 5] │ [39] [40] [41] [42] [43] [44] │
├─────────────────────────────────────────────────────────┤
│ [🔍 Фильтры]                    [Сортировка ▼]         │
└─────────────────────────────────────────────────────────┘
```

---

## Статус: ✅ Полностью выполнено

**Изменено:**
- `/views/catalog/index.php` — HTML структура + стили + JavaScript

**Результат:**
1. Заголовок со счетчиком в одну строку — компактно
2. Быстрые фильтры по брендам и размерам — удобно
3. Полная адаптивность под все разрешения
4. Улучшен UX каталога для быстрого поиска товаров

Обновите страницу каталога чтобы увидеть изменения! 🎯
