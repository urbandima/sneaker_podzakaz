# Руководство по тестированию десктопной версии каталога

## Быстрый старт

### 1. Запуск сервера разработки
```bash
cd /Users/user/CascadeProjects/splitwise
php -S localhost:8080 -t web/
```

### 2. Откройте каталог в браузере
```
http://localhost:8080/catalog?gender=male
```

### 3. Очистите кэш браузера
**Важно!** Обязательно очистите кэш, чтобы увидеть новые стили:

**Chrome/Edge:**
- `Cmd+Shift+R` (Mac) или `Ctrl+Shift+R` (Windows)
- Или: DevTools → Network → Disable cache

**Safari:**
- `Cmd+Option+E` — очистить кэш
- `Cmd+R` — перезагрузить страницу

**Firefox:**
- `Cmd+Shift+R` (Mac) или `Ctrl+Shift+R` (Windows)

## Что проверять

### Визуальная проверка

#### ✅ На экране 1920×1080 (Full HD)
**Должно быть:**
- Quick filters (бренды) — **компактные чипы** в одну строку
- Размеры (EU/US/UK/CM) — переключатель и размеры **в одну строку**
- Отступы между элементами — **уменьшенные**
- Товары — **видно больше без скролла**

**Проверьте отступы:**
```
Breadcrumbs ───┐
               ├── 0.625rem (10px)
Quick Filters ─┤
               ├── 0.375rem (6px)
Размеры ───────┤
               ├── 0.375rem (6px)
Toolbar ───────┤
               ├── 0.375rem (6px)
Товары ────────┘
```

#### ✅ На экране 1280×800 (MacBook 13")
**Должно быть:**
- Размеры в одну строку с переключателем
- Компактные чипы брендов
- Отступы немного больше, чем на Full HD

#### ✅ На экране 1024×768 (минимальный десктоп)
**Должно быть:**
- Размеры переключились в строку
- Чипы стали компактнее
- Все еще читаемо и удобно

### Функциональная проверка

#### 1. Фильтры работают
- [ ] Клик по бренду (New Balance, Nike) — товары фильтруются
- [ ] Переключение размеров (EU → US → UK → CM) — работает
- [ ] Клик по размеру — фильтрует товары
- [ ] Кнопка "Фильтры" — открывает sidebar

#### 2. Адаптивность
- [ ] Измените ширину окна от 1920px до 1024px
- [ ] Все элементы плавно адаптируются
- [ ] Нет горизонтального скролла
- [ ] Читаемость сохранена

#### 3. Responsive на разных разрешениях
```
< 1024px    → мобильная/планшет (без изменений)
1024px      → размеры в строку, компактность
1280px      → дополнительная компактность
1536px      → максимальная компактность
```

### Проверка в DevTools

#### Chrome DevTools
1. Откройте DevTools (`F12`)
2. Toggle Device Toolbar (`Cmd+Shift+M`)
3. Проверьте разрешения:
   - Desktop: 1920×1080
   - MacBook Pro: 1440×900
   - MacBook Air: 1280×800

#### Проверка CSS
```css
/* Откройте Elements → Styles */

/* На 1920px должно быть: */
.quick-filters-bar {
    margin-bottom: 0.25rem;  /* ~4px */
    padding: 0.25rem 0;      /* ~4px */
}

.quick-filters-sizes {
    flex-direction: row;     /* в строку */
    margin-bottom: 0.25rem;
}

.breadcrumbs {
    padding: 0.5rem 0;       /* ~8px */
}
```

## Сравнение: До vs После

### Вертикальное пространство

**ДО (старая версия):**
```
Breadcrumbs (1rem)        ─┬─ 16px
                           │
Quick Filters (0.75rem)   ─┼─ 12px
                           │
Размеры (0.75rem)         ─┼─ 12px
                           │
Toolbar (0.75rem)         ─┼─ 12px
                           │
Header (0.5rem)           ─┼─ 8px
                           │
Товары                    ─┴─
                           
ИТОГО: ~60px отступов
```

**ПОСЛЕ (новая версия на 1920px):**
```
Breadcrumbs (0.5rem)      ─┬─ 8px
                           │
Quick Filters (0.25rem)   ─┼─ 4px
                           │
Размеры (0.25rem)         ─┼─ 4px
                           │
Toolbar (0.375rem)        ─┼─ 6px
                           │
Header (0.25rem)          ─┼─ 4px
                           │
Товары                    ─┴─
                           
ИТОГО: ~26px отступов

ЭКОНОМИЯ: 34px (57%)
```

### Визуальное сравнение

**ДО:**
- 🔽 Много пустого пространства
- 📏 Размеры занимали 2 строки
- 📦 Видно 10-12 товаров без скролла

**ПОСЛЕ:**
- ✅ Компактная верстка
- ➡️ Размеры в одну строку
- 📦 Видно 12-14 товаров без скролла (+20%)

## Частые проблемы

### Проблема: Старые стили все еще видны
**Решение:**
1. Проверьте URL файла CSS в DevTools → Network
2. Должен быть: `catalog-inline.css?v=2.2`
3. Если нет — очистите кэш (`Cmd+Shift+R`)

### Проблема: Размеры не в одну строку на 1920px
**Решение:**
1. Проверьте ширину окна браузера (должна быть ≥1024px)
2. Откройте DevTools → Elements → `.quick-filters-sizes`
3. Должно быть: `flex-direction: row`

### Проблема: Отступы не изменились
**Решение:**
1. Проверьте разрешение экрана (должно быть ≥1024px)
2. Проверьте медиа-запросы в DevTools:
   ```css
   @media (min-width: 1024px) { /* должен быть активен */ }
   ```

## Метрики успеха

### ✅ Чек-лист тестирования

- [ ] **Визуально компактнее** — меньше пустого пространства
- [ ] **Размеры в одну строку** на ≥1024px
- [ ] **Чипы меньше** — padding 0.4rem вместо 0.5rem
- [ ] **Больше товаров** видно без скролла (+2-3 товара)
- [ ] **Фильтры работают** — бренды, размеры, сортировка
- [ ] **Адаптивно** — плавный переход между разрешениями
- [ ] **Мобильная версия** не сломалась (< 1024px)

### Производительность

**DevTools → Performance:**
- [ ] Layout Shift < 0.1 (нет визуальных "прыжков")
- [ ] CSS загружается быстро (< 100ms)
- [ ] Нет лишних запросов к старым файлам

**DevTools → Lighthouse:**
- [ ] Performance ≥ 90
- [ ] Best Practices ≥ 90
- [ ] Accessibility ≥ 90

## Откат изменений (если что-то не так)

### Быстрый откат CSS
```php
// views/catalog/index.php
// Измените версию обратно
$this->registerCssFile('@web/css/catalog-inline.css?v=2.1');
```

### Полный откат через Git
```bash
git status
git diff web/css/catalog-inline.css
git checkout web/css/catalog-inline.css
git checkout views/catalog/index.php
```

## Контакты для отчета о багах

Если нашли проблему — создайте issue с описанием:
1. **Разрешение экрана** (например: 1920×1080)
2. **Браузер** (Chrome 120, Safari 17, etc.)
3. **Скриншот** проблемы
4. **Ожидаемое поведение** vs **Фактическое**

---

**Документация создана:** 2025-11-09  
**Версия CSS:** v=2.2  
**Файлы:** catalog-inline.css, catalog/index.php
