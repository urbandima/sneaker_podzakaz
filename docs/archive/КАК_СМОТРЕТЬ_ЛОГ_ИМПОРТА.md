# 📋 Как смотреть лог импорта Poizon

## 🚀 Быстрый старт

### Способ 1: Консольный импорт (видно всё сразу)

```bash
php yii poizon-import-json/run "ПУТЬ_К_ФАЙЛУ.json" --verbose=1
```

**Сразу видно**:
- Проверку файла
- Структуру JSON (сколько категорий, брендов, товаров)
- Импорт каждого товара
- Все ошибки с деталями
- Статистику

---

### Способ 2: Веб-импорт (через админку)

1. Откройте `/admin/poizon-run`
2. Перетащите JSON файл
3. Нажмите "Загрузить и импортировать"
4. Смотрите лог:

```bash
# Все логи веб-импорта
ls -lht runtime/logs/poizon-web-import-*.log

# Последний лог
tail -f runtime/logs/poizon-web-import-*.log
```

---

## 🔍 Где искать логи

```bash
# Все логи импорта
ls -lht runtime/logs/poizon-*.log

# Последний лог (любой)
ls -t runtime/logs/poizon-*.log | head -n 1

# Смотреть последний лог
cat $(ls -t runtime/logs/poizon-*.log | head -n 1)

# Смотреть лог в реальном времени
tail -f runtime/logs/poizon-*.log
```

---

## 📊 Что видно в логе

```
[2024-11-04 23:38:00] ╔══════════════════════════════════════════════════════╗
[2024-11-04 23:38:00] ║  🚀 ИМПОРТ ТОВАРОВ ИЗ POIZON JSON                  ║
[2024-11-04 23:38:00] ╚══════════════════════════════════════════════════════╝
[2024-11-04 23:38:00] 📡 Загружаю JSON из: /path/to/file.json
[2024-11-04 23:38:01] Проверка локального файла: /path/to/file.json
[2024-11-04 23:38:01] Файл найден. Размер: 2621440 bytes
[2024-11-04 23:38:02] JSON загружен. Размер: 2621440 bytes
[2024-11-04 23:38:02] JSON распарсен успешно
[2024-11-04 23:38:02] Структура JSON: categories=5, brands=12, products=100
[2024-11-04 23:38:03] Начинаю импорт категорий
[2024-11-04 23:38:04] Категории импортированы: 5
[2024-11-04 23:38:04] Начинаю импорт брендов
[2024-11-04 23:38:05] Бренды импортированы: 12
[2024-11-04 23:38:05] Начинаю импорт товаров. Всего: 100
[2024-11-04 23:38:06] [1/100] Импорт: Nike Air Max (ID: 123456)
[2024-11-04 23:38:07]      Успешно импортирован
```

---

## ❌ Если есть ошибки

### Найти все ошибки

```bash
grep "ОШИБКА" runtime/logs/poizon-*.log
```

### Найти ошибки в последнем логе

```bash
cat $(ls -t runtime/logs/poizon-*.log | head -n 1) | grep "ОШИБКА" -A 10
```

### Посмотреть детали ошибки

```bash
# Откроется лог, ищите слово "ОШИБКА"
less $(ls -t runtime/logs/poizon-*.log | head -n 1)

# Нажмите "/" и введите "ОШИБКА" для поиска
# Нажмите "n" для следующего совпадения
# Нажмите "q" для выхода
```

---

## 💡 Полезные команды

### Показать структуру JSON файла

```bash
cat file.json | jq '{
  categories: (.categories | length),
  brands: (.brands | length), 
  products: (.products | length)
}'
```

### Показать первый товар из JSON

```bash
cat file.json | jq '.products[0]'
```

### Проверить, есть ли товары

```bash
cat file.json | jq '.products | length'
```

### Найти проблемный товар по ID

```bash
grep "Product ID: 123456" runtime/logs/poizon-*.log -A 20
```

---

## 🎯 Чек-лист отладки

- [ ] Запустил `php yii poizon-import-json/run "файл" --verbose=1`
- [ ] Посмотрел структуру JSON (категории, бренды, товары)
- [ ] Проверил, есть ли ошибки в консоли
- [ ] Открыл лог-файл `runtime/logs/poizon-*.log`
- [ ] Нашел первую ошибку в логе
- [ ] Посмотрел данные проблемного товара
- [ ] Исправил проблему
- [ ] Запустил повторно

---

## 🚀 Готово!

Теперь вы знаете, как:
- ✅ Запустить импорт с детальным выводом
- ✅ Найти и посмотреть логи
- ✅ Найти ошибки в логе
- ✅ Отладить проблемы импорта

**Запустите импорт**:
```bash
php yii poizon-import-json/run "ВАШ_ФАЙЛ.json" --verbose=1
```

Всё будет видно! 📊
