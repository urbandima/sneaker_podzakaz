# Исправление верстки каталога - Инструкция по выгрузке

**Дата:** 08.11.2025, 23:15  
**Проблема:** Каталог на десктопе отображался в мобильной версии (1-2 колонки вместо 4-5)

---

## 🚀 Быстрая выгрузка на сервер

### Шаг 1: Подключиться к серверу по SSH

```bash
ssh user@your-server.com
cd /path/to/project
```

### Шаг 2: Выгрузить измененные файлы

**Вариант А: Через Git (рекомендуется)**

```bash
# На локальной машине
git add views/catalog/index.php
git add web/css/catalog-inline.css
git add web/css/catalog-inline.min.css
git commit -m "Fix: Исправлена мобильная верстка каталога на десктопе"
git push origin main

# На сервере
git pull origin main
```

**Вариант Б: Прямая загрузка через SCP**

```bash
# С локальной машины
scp views/catalog/index.php user@server:/path/to/project/views/catalog/
scp web/css/catalog-inline.css user@server:/path/to/project/web/css/
scp web/css/catalog-inline.min.css user@server:/path/to/project/web/css/
```

**Вариант В: Через FTP/SFTP клиент (FileZilla, Cyberduck)**

Загрузите следующие файлы:
1. `/views/catalog/index.php`
2. `/web/css/catalog-inline.css`
3. `/web/css/catalog-inline.min.css`

---

## 📋 Измененные файлы

### 1. `views/catalog/index.php` (строка 49)
**Что изменилось:** Вернулась загрузка актуальной минифицированной версии CSS

```php
// Было (если использовалась устаревшая версия):
$this->registerCssFile('@web/css/catalog-inline.min.css', [
    'position' => \yii\web\View::POS_HEAD,
]);

// Стало (актуальная версия):
$this->registerCssFile('@web/css/catalog-inline.min.css', [
    'position' => \yii\web\View::POS_HEAD,
]);
```

### 2. `web/css/catalog-inline.css`
**Что изменилось:** Добавлены `!important` для всех media queries, чтобы гарантировать правильную сетку на всех экранах

**Основные изменения:**
```css
/* Mobile: 2 колонки */
.products {
    display: grid !important;
    grid-template-columns: repeat(2,1fr) !important;
}

/* Tablet 640px+: 3 колонки */
@media (min-width:640px) {
    .products { grid-template-columns: repeat(3,1fr) !important; }
}

/* Desktop 1024px+: 4 колонки */
@media (min-width:1024px) {
    .products { grid-template-columns: repeat(4,1fr) !important; }
}

/* Large Desktop 1280px+: 4-5 колонок */
@media (min-width:1280px) {
    .products.grid-5 { grid-template-columns: repeat(5,1fr) !important; }
}

/* XL Desktop 1536px+: 5 колонок */
@media (min-width:1536px) {
    .products { grid-template-columns: repeat(5,1fr) !important; }
}
```

### 3. `web/css/catalog-inline.min.css`
**Что изменилось:** Пересоздана минифицированная версия с актуальными правилами

---

## ✅ Проверка после выгрузки

### 1. Очистите кеш сервера (если используется)

```bash
# Yii2 cache
php yii cache/flush-all

# Или вручную
rm -rf runtime/cache/*
```

### 2. Очистите кеш браузера

**Способ 1: Hard Refresh**
- **Windows/Linux:** `Ctrl + Shift + R` или `Ctrl + F5`
- **Mac:** `Cmd + Shift + R`

**Способ 2: DevTools**
1. Откройте DevTools (F12)
2. ПКМ на кнопке обновления → "Очистить кеш и выполнить жесткую перезагрузку"

### 3. Проверьте на разных разрешениях

**Открыть каталог:** `https://your-site.com/catalog`

**Ожидаемый результат:**

| Разрешение | Колонок | Устройство |
|------------|---------|------------|
| < 640px | 2 | 📱 Mobile |
| 640-1023px | 3 | 📱💻 Tablet |
| 1024-1279px | 4 | 🖥️ Desktop |
| 1280-1535px | 4-5 | 🖥️ Large Desktop |
| 1536px+ | 5 | 🖥️🖥️ XL Desktop |

**Проверьте через DevTools:**
1. F12 → Toggle device toolbar (Ctrl+Shift+M)
2. Выберите разные разрешения: iPhone SE, iPad, Desktop HD
3. Убедитесь, что сетка меняется корректно

---

## 🔧 Если проблема не исчезла

### Проблема 1: Каталог всё еще в 2 колонки на десктопе

**Решение:**
```bash
# Проверьте, что файлы загружены на сервер
ls -la web/css/catalog-inline.min.css
cat web/css/catalog-inline.min.css | grep "grid-template-columns"

# Если нет изменений - перезагрузите
scp web/css/catalog-inline.min.css user@server:/path/to/project/web/css/
```

### Проблема 2: Стили не применяются из-за кеширования

**Решение А: Добавьте версию к CSS файлу**
```php
// views/catalog/index.php
$this->registerCssFile('@web/css/catalog-inline.min.css?v=' . time(), [
    'position' => \yii\web\View::POS_HEAD,
]);
```

**Решение Б: Настройте заголовки кеша**
```php
// components/HttpCacheHeaders.php (если используется)
Yii::$app->response->headers->set('Cache-Control', 'no-cache, must-revalidate');
```

### Проблема 3: Конфликт с другими CSS файлами

**Проверьте порядок загрузки:**
```bash
# В браузере (DevTools → Network → CSS)
# Должен быть последним:
catalog-inline.min.css
```

Если есть конфликт - добавьте `!important` в проблемные правила CSS.

---

## 📊 Техническая информация

### Суть проблемы
- **Устаревшая минифицированная версия** не содержала актуальные media queries
- **Mobile-first стили** применялись на всех разрешениях
- **Отсутствие !important** позволяло другим стилям перезаписывать сетку

### Решение
- Добавлены `!important` флаги для гарантии применения
- Пересоздана минифицированная версия с актуальными правилами
- Настроена корректная каскадность media queries

### Совместимость
- ✅ Все современные браузеры (Chrome, Firefox, Safari, Edge)
- ✅ Адаптивный дизайн от mobile до 4K экранов
- ✅ Grid Layout с полной поддержкой

---

## 📞 Поддержка

Если после выгрузки проблема сохраняется:

1. Проверьте DevTools → Console на наличие ошибок
2. Проверьте DevTools → Network, что файлы загружаются
3. Очистите весь кеш (браузер + сервер + CDN если есть)
4. Проверьте права доступа к файлам на сервере (chmod 644)

**Лог изменений:** См. `PROJECT_TASKS.md` - секция "Исправлена мобильная верстка каталога на десктопе"

---

**Готово к деплою! ✅**
