# Чек-лист тестирования после очистки кода

**Дата:** 07.11.2025  
**Статус:** Готово к тестированию

---

## Критические функции (проверить в первую очередь)

### 1. Каталог товаров (`/catalog/`)

- [ ] Фильтры по брендам работают
- [ ] Фильтры по категориям работают
- [ ] Фильтры по размерам работают (EU/US/UK/CM)
- [ ] Ценовой слайдер работает
- [ ] Quick-filters (чипы размеров) работают
- [ ] Карточки товаров отображаются корректно
- [ ] Свайп изображений на мобильных работает
- [ ] Кнопка "В избранное" добавляет товар
- [ ] Кнопка "Подробнее" ведёт на страницу товара
- [ ] Пагинация работает
- [ ] Infinite scroll работает (если включен)

### 2. Страница товара (`/catalog/product/slug`)

- [ ] Галерея изображений свайпается на мобильных
- [ ] Миниатюры переключают большое изображение
- [ ] Выбор размера работает
- [ ] Цена обновляется при выборе размера
- [ ] Sticky панель появляется при скролле
- [ ] Кнопка "Быстрый заказ" открывает модальное окно
- [ ] Форма быстрого заказа отправляется
- [ ] Email менеджеру приходит
- [ ] Telegram уведомление приходит (если настроено)
- [ ] Кнопка "В корзину" работает
- [ ] Кнопка "В избранное" работает
- [ ] Похожие товары отображаются

### 3. Корзина (`/cart/`)

- [ ] Товары в корзине отображаются
- [ ] Изменение количества работает
- [ ] Удаление товара работает
- [ ] Итоговая сумма считается правильно
- [ ] Кнопка "Оформить заказ" работает

### 4. Избранное (`/catalog/favorites`)

- [ ] Список избранных товаров отображается
- [ ] Удаление из избранного работает
- [ ] Счётчик избранного обновляется

---

## Проверка консоли браузера

Открой **DevTools → Console** и проверь отсутствие ошибок:

### ❌ Не должно быть таких ошибок:

```
Uncaught ReferenceError: toggleCompare is not defined
Uncaught ReferenceError: quickAddToCart is not defined
Uncaught ReferenceError: selectQuickSize is not defined
Uncaught ReferenceError: openQuickView is not defined
Uncaught SyntaxError: Unexpected token 'if'
```

### ✅ Допустимые предупреждения:

```
[Deprecation] Synchronous XMLHttpRequest... (игнорируй)
Mixed Content: The page at... (если используешь http вместо https)
```

---

## Проверка производительности (Lighthouse)

Запусти **DevTools → Lighthouse** для главной страницы и страницы товара:

### Минимальные требования:

- **Performance:** > 75
- **Accessibility:** > 85
- **Best Practices:** > 85
- **SEO:** > 90

### Критические метрики:

- **First Contentful Paint (FCP):** < 2.5s
- **Largest Contentful Paint (LCP):** < 3.5s
- **Total Blocking Time (TBT):** < 500ms
- **Cumulative Layout Shift (CLS):** < 0.1

---

## Проверка на разных устройствах

### Desktop (1920x1080):
- [ ] Каталог: 4 колонки товаров
- [ ] Фильтры в левой колонке
- [ ] Sticky header работает
- [ ] Hover-эффекты работают

### Tablet (768x1024):
- [ ] Каталог: 3 колонки товаров
- [ ] Фильтры открываются через боковую панель
- [ ] Свайп галереи работает

### Mobile (375x667):
- [ ] Каталог: 2 колонки товаров
- [ ] Фильтры открываются на весь экран
- [ ] Свайп карточек товаров работает
- [ ] Sticky панель на странице товара работает
- [ ] Кнопки удобно нажимать (минимум 44x44px)

---

## Проверка сетевых запросов

Открой **DevTools → Network** и проверь:

### AJAX фильтрация:
- [ ] POST `/catalog/filter` возвращает 200 OK
- [ ] Response содержит `{"success": true, "html": "...", "products": [...]}`
- [ ] Время ответа < 1000ms

### Quick Order:
- [ ] POST `/catalog/quick-order` возвращает 200 OK
- [ ] Request body: `{"product_id": 1, "name": "...", "phone": "...", "size": "..."}`
- [ ] Response: `{"success": true, "message": "..."}`

### Избранное:
- [ ] POST `/catalog/add-favorite` возвращает 200 OK
- [ ] POST `/catalog/remove-favorite` возвращает 200 OK

---

## Проверка email уведомлений

### Quick Order:
1. Заполни форму быстрого заказа
2. Нажми "Оформить заказ"
3. Проверь почту менеджера:
   - [ ] Тема: "⚡ Быстрый заказ: [название товара]"
   - [ ] Содержит: имя клиента, телефон, товар, размер, цену, ссылку
   - [ ] Корректная кодировка (нет кракозябров)

### Telegram (если настроено):
- [ ] Сообщение пришло в чат менеджера
- [ ] Форматирование корректное
- [ ] Ссылка на товар кликабельна

---

## Проверка базы данных

Если есть доступ к MySQL/PostgreSQL, проверь:

```sql
-- Проверка колонки views_count
SELECT id, name, views_count FROM product WHERE id = 1;

-- Проверка размеров
SELECT ps.product_id, ps.eu_size, ps.us_size, ps.is_available
FROM product_size ps
WHERE ps.product_id = 1;

-- Проверка избранного
SELECT pf.user_id, pf.product_id, p.name
FROM product_favorite pf
JOIN product p ON p.id = pf.product_id
WHERE pf.user_id = 1;
```

Ожидаемый результат:
- [ ] Колонка `views_count` существует и содержит числа
- [ ] Размеры корректно связаны с товарами
- [ ] Избранное записывается в БД

---

## Тестовые сценарии

### Сценарий 1: Покупка товара
1. Зайди на `/catalog/`
2. Выбери фильтр по бренду (например, Nike)
3. Кликни на карточку товара
4. Выбери размер
5. Нажми "Добавить в корзину"
6. Зайди в корзину
7. Оформи заказ

**Ожидаемый результат:** Заказ создан, email отправлен менеджеру.

### Сценарий 2: Быстрый заказ
1. Зайди на страницу товара `/catalog/product/nike-air-max`
2. Выбери размер
3. Проскроль вниз (появится sticky панель)
4. Нажми "Быстрый заказ" в sticky панели
5. Заполни форму
6. Нажми "Оформить заказ"

**Ожидаемый результат:** Email с заказом отправлен, модальное окно показывает "Заказ оформлен!".

### Сценарий 3: Фильтрация
1. Зайди на `/catalog/`
2. Выбери бренд "Nike"
3. Выбери размер "42 EU"
4. Установи цену от 100 до 500 BYN
5. Проверь URL: должен быть SEF вида `/catalog/filter/brand-nike/size-42-eu/price-100-500/`

**Ожидаемый результат:** Отображаются только товары Nike размера 42 в диапазоне цен.

---

## Готово к production, если:

- ✅ Все критические функции работают
- ✅ Нет ошибок в консоли браузера
- ✅ Lighthouse Performance > 75
- ✅ Работает на Desktop, Tablet, Mobile
- ✅ Email/Telegram уведомления приходят
- ✅ База данных корректна (нет Integrity constraint violation)

---

**Следующий шаг:** Запусти smoke-тесты и переводи на production 🚀
