# 🚀 ЗАПУСК СИСТЕМЫ ИМПОРТА POIZON - ПОШАГОВАЯ ИНСТРУКЦИЯ

## ⚡ Что нужно сделать СЕЙЧАС (критично)

### Шаг 1: Применить миграции БД (2 минуты)

Откройте терминал и выполните:

```bash
cd /Users/user/CascadeProjects/splitwise
php yii migrate
```

Вы увидите:
```
*** applying m251104_120000_add_poizon_fields_to_product
*** applied m251104_120000_add_poizon_fields_to_product (time: 0.045s)

*** applying m251104_120100_update_product_size_with_grids
*** applied m251104_120100_update_product_size_with_grids (time: 0.032s)

*** applying m251104_120200_create_import_tables
*** applied m251104_120200_create_import_tables (time: 0.056s)

Migrated up successfully.
```

✅ **Готово!** База данных обновлена.

---

### Шаг 2: Получить доступ к данным Poizon (критично!)

**У вас СЕЙЧАС нет готового инструмента для парсинга Poizon.**

Вам нужно выбрать ОДИН из вариантов:

#### Вариант A: XML фид (рекомендуется) 💰

**Что это:** Готовый XML файл с товарами Poizon, обновляется автоматически.

**Где взять:**
1. Сервисы парсинга (платные):
   - q-parser.ru
   - parser24.ru
   - другие парсеры Dewu/Poizon

2. Стоимость: ~30-100$ в месяц

3. Получите URL вида:
   ```
   https://s3.q-parser.ru/export/12345/poizon.xml
   ```

**Настройка:**
```php
// config/params.php
'poizonXmlUrl' => 'https://s3.q-parser.ru/export/12345/poizon.xml',
```

#### Вариант B: Сторонний API парсинга 💰

**Что это:** Готовый API для доступа к данным Poizon.

**Где взять:**
- Поиск: "Poizon API", "Dewu API", "Poizon parser API"
- Примеры: DewuParser, Poizon Parser Service

**Настройка:**
```php
// config/params.php
'poizonApiUrl' => 'https://api.poizon-parser.com/v1',
'poizonApiKey' => 'ваш_api_ключ',
```

#### Вариант C: Собственный парсер 🔧

**Что это:** Разработка парсера сайта Poizon с нуля.

**Сложность:** Высокая (требует знаний Python/Node.js, опыт парсинга)

**Не рекомендуется** без технических навыков.

---

### Шаг 3: Настроить параметры (1 минута)

Откройте файл `config/params.php` и настройте:

```php
// URL источника данных (ОБЯЗАТЕЛЬНО!)
'poizonXmlUrl' => 'ВАШ_URL_ЗДЕСЬ', // Или null, если используете API

// API ключ (если требуется)
'poizonApiKey' => null,

// Курс CNY -> BYN (обновите актуальный курс!)
'cnyToBynRate' => 0.45, // 1 юань = 0.45 белорусских рублей
```

**Как узнать курс CNY -> BYN:**
- Google: "1 cny to byn"
- Банки: НБ РБ, Беларусбанк
- Примерный курс: 0.40 - 0.50

---

### Шаг 4: Проверить подключение (30 секунд)

```bash
php yii poizon-import/test
```

**Если ОК:**
```
✅ Подключение успешно!
Найдено товаров: 1234
```

**Если ошибка:**
```
❌ Ошибка подключения: Failed to load XML feed
```

➡️ **Решение:** Проверьте URL в `params.php`, убедитесь, что он доступен.

---

### Шаг 5: Первый импорт (10-30 минут)

#### Тестовый импорт (10 товаров)

```bash
php yii poizon-import/run --limit=10
```

Проверьте результат:
- Админка: `/admin-import`
- Каталог: `/catalog`

#### Импорт 100 товаров (для проверки)

```bash
php yii poizon-import/run --limit=100
```

#### Полный импорт (все товары)

```bash
php yii poizon-import/run --limit=0
```

⚠️ **ВАЖНО:** Первый полный импорт может занять 1-2 часа для 10,000 товаров.

---

### Шаг 6: Проверить результаты (2 минуты)

**1. Админ-панель импорта:**
```
https://ваш-сайт.by/admin-import
```

**2. Каталог товаров:**
```
https://ваш-сайт.by/catalog
```

**3. База данных (через phpMyAdmin или консоль):**
```sql
-- Сколько товаров импортировано
SELECT COUNT(*) FROM product WHERE sku LIKE 'POIZON-%';

-- Последний импорт
SELECT * FROM import_batch ORDER BY id DESC LIMIT 1;

-- Есть ли ошибки
SELECT * FROM import_log WHERE action = 'error' LIMIT 10;
```

---

### Шаг 7: Настроить автоматическое обновление (5 минут)

#### Вариант A: Через crontab (Linux/Mac)

```bash
# Открыть crontab
crontab -e

# Добавить строку (каждый день в 3:00 ночи)
0 3 * * * cd /Users/user/CascadeProjects/splitwise && php yii poizon-import/run --limit=0 >> runtime/logs/poizon-import.log 2>&1
```

**Сохранить и выйти:** `ESC` → `:wq` → `Enter`

#### Вариант B: Через готовый скрипт

```bash
# Отредактировать путь в скрипте
nano poizon-import-cron.sh

# Найти строку PROJECT_PATH и изменить на свой путь:
PROJECT_PATH="/Users/user/CascadeProjects/splitwise"

# Сделать исполняемым
chmod +x poizon-import-cron.sh

# Добавить в crontab
crontab -e
# Добавить:
0 3 * * * /Users/user/CascadeProjects/splitwise/poizon-import-cron.sh
```

#### Проверить работу cron

```bash
# Посмотреть активные задачи
crontab -l

# Посмотреть логи
tail -f /Users/user/CascadeProjects/splitwise/runtime/logs/poizon-import.log
```

---

## ✅ ГОТОВО!

Система настроена и работает.

---

## 📱 Как пользоваться системой

### Через веб-интерфейс (без консоли)

1. Откройте `/admin-import`
2. Нажмите **"Запустить импорт"**
3. Укажите лимит товаров (например, 500)
4. Нажмите **"Запустить импорт"**
5. Импорт запустится в фоне
6. Обновите страницу через 5 минут
7. Проверьте результаты

### Через консоль (для админов)

```bash
# Импорт 500 товаров
php yii poizon-import/run --limit=500

# Импорт всех товаров
php yii poizon-import/run --limit=0

# Тестовый режим (не сохраняет в БД)
php yii poizon-import/run --limit=50 --dryRun=1

# Посмотреть логи
php yii poizon-import/logs

# Посмотреть детали импорта #5
php yii poizon-import/logs 5
```

---

## 🔍 Как проверить, что всё работает

### ✅ Товары импортируются

```sql
SELECT name, sku, price, brand_id FROM product 
WHERE sku LIKE 'POIZON-%' 
ORDER BY id DESC 
LIMIT 10;
```

### ✅ Размеры конвертируются

```sql
SELECT 
    ps.size, 
    ps.us_size, 
    ps.eu_size, 
    ps.uk_size, 
    ps.cm_size,
    p.name
FROM product_size ps
JOIN product p ON ps.product_id = p.id
WHERE p.sku LIKE 'POIZON-%'
LIMIT 10;
```

### ✅ Логи пишутся

```sql
-- Последние 10 импортов
SELECT * FROM import_batch ORDER BY id DESC LIMIT 10;

-- Успешность последних 5 импортов
SELECT 
    id,
    status,
    created_count,
    updated_count,
    error_count,
    ROUND((created_count + updated_count) / total_items * 100, 1) as success_rate
FROM import_batch 
ORDER BY id DESC 
LIMIT 5;
```

---

## ❌ Что-то пошло не так?

### Ошибка: "Poizon API Error"

**Причина:** Нет доступа к данным Poizon

**Решение:**
1. Проверьте `config/params.php` → `poizonXmlUrl`
2. Откройте URL в браузере, должен открыться XML
3. Если не открывается - обратитесь к поставщику фида

### Ошибка: "Failed to load XML"

**Причина:** XML фид недоступен или неверный формат

**Решение:**
1. Проверьте URL: `curl -I ваш-url`
2. Проверьте формат XML
3. Убедитесь, что нет ошибок в XML

### Ошибка: "Validation error"

**Причина:** Данные товара некорректны

**Решение:**
1. Посмотрите детали: `php yii poizon-import/logs`
2. Проверьте, что в XML есть обязательные поля: name, price, brand

### Импорт очень медленный

**Причина:** Большое количество товаров или медленный API

**Решение:**
1. Разбейте на части: `--limit=500` несколько раз
2. Увеличьте `max_execution_time` в PHP
3. Используйте более быстрый сервер

### Дубликаты товаров

**Причина:** SKU не уникальны

**Решение:**
```sql
-- Найти дубликаты
SELECT sku, COUNT(*) FROM product 
GROUP BY sku 
HAVING COUNT(*) > 1;

-- Удалить дубликаты (осторожно!)
-- DELETE FROM product WHERE id IN (...);
```

---

## 📞 Куда обращаться

**Документация:**
- `POIZON_QUICK_START.md` - 5 шагов за 30 минут
- `POIZON_INTEGRATION.md` - Полное руководство
- `POIZON_README.md` - Технический обзор

**Логи:**
```bash
# Консольные
php yii poizon-import/logs

# Файловые
tail -f runtime/logs/app.log
tail -f runtime/logs/poizon-import.log

# В админке
/admin-import → История импортов → Детали
```

---

## 🎯 Контрольный список

- [ ] **Шаг 1:** Миграции применены (`php yii migrate`)
- [ ] **Шаг 2:** Есть доступ к данным Poizon (XML или API)
- [ ] **Шаг 3:** Настроены параметры в `config/params.php`
- [ ] **Шаг 4:** Тест подключения успешен (`php yii poizon-import/test`)
- [ ] **Шаг 5:** Первый импорт выполнен (`php yii poizon-import/run --limit=100`)
- [ ] **Шаг 6:** Товары отображаются в каталоге
- [ ] **Шаг 7:** Настроен cron для автоматизации

---

## 🎉 Готово!

**Система импорта Poizon работает!**

Теперь товары будут автоматически обновляться каждый день.

**Следующие действия:**
1. Проверяйте админку `/admin-import` раз в неделю
2. Обновляйте курс CNY в `params.php` при изменении
3. Следите за логами ошибок

**Вопросы?** Читайте `POIZON_INTEGRATION.md` - там всё подробно описано.

---

**Удачи! 🚀**
