# 🚀 Руководство по импорту товаров из Poizon

## 📋 Что было сделано

### 1. Добавлены новые поля в Product
- `vendor_code` - артикул производителя
- `country_of_origin` - страна происхождения
- `favorite_count` - количество добавлений в избранное
- `properties` (JSON) - характеристики товара
- `sizes_data` (JSON) - размерные сетки
- `purchase_price` - закупочная цена
- `delivery_time_min/max` - время доставки
- `keywords` (JSON) - ключевые слова
- `series_name` - название серии
- `parent_product_id` - ID родительского товара (для вариантов)
- `poizon_variant_id` - ID варианта в Poizon
- `variant_params` (JSON) - параметры варианта (цвет, размер)
- `stock_count` - количество на складе

### 2. Добавлены poizon_id для Category и Brand
- Для связи с ID категорий/брендов в Poizon

### 3. Создан новый импортер JSON
- `commands/PoizonImportJsonController.php`
- Импортирует товары из JSON файла Poizon
- Поддерживает родительские товары и варианты (children)

### 4. Улучшена админка товаров
- Показ закупочной цены и наценки
- Отображение времени доставки
- Количество вариантов товара
- Количество в избранном
- Страна происхождения
- Артикул производителя
- Визуальные бейджи (Poizon, Limited, Variant)

---

## ⚡ Инструкция по применению

### Шаг 1: Применить миграции

```bash
cd /Users/user/CascadeProjects/splitwise

# Применить миграции
php yii migrate --interactive=0
```

Будут применены 3 новые миграции:
- `m251104_220000_add_missing_poizon_fields` - новые поля для Product
- `m251104_220100_add_poizon_id_to_category_brand` - poizon_id для Category/Brand

---

### Шаг 2: Запустить импорт из JSON

```bash
# Импорт из JSON файла Poizon
php yii poizon-import-json/run "https://storage.yandexcloud.net/poizon-exports/site/81988f42-8231-43d7-b4ed-bd70ddf56f06/c8cb6cd8-8970-4b01-976f-9407dea04f9c/export_04_11_2025__15_32_15.json"
```

#### Что произойдет:

1. **Импорт категорий** из JSON
   - Создаст категории с иерархией (parent/child)
   - Сохранит poizon_id для связи

2. **Импорт брендов** из JSON
   - Создаст бренды с логотипами
   - Сохранит poizon_id

3. **Импорт товаров**
   - Для каждого товара создаст:
     - **Родительский товар** (общая информация)
     - **Варианты** (children) - для каждого размера/цвета
   - Импортирует изображения
   - Парсит характеристики из properties[]
   - Сохраняет размерные сетки

4. **Вывод статистики**:
```
╔══════════════════════════════════════════════════════╗
║  📊 СТАТИСТИКА ИМПОРТА                              ║
╚══════════════════════════════════════════════════════╝

✅ Категорий создано:        3
✅ Брендов создано:          1
✅ Товаров создано:          4
✅ Товаров обновлено:        0
✅ Вариантов создано:        87
✅ Изображений создано:      24

🎉 Импорт завершен!
```

---

## 📦 Структура импорта

### Родительский товар
Содержит общую информацию:
- Название, описание
- Бренд, категория
- Основная цена
- Характеристики (properties)
- Размерные сетки (sizes)
- Изображения

### Варианты (children)
Создаются для каждого размера/цвета:
- Связаны с родительским товаром (`parent_product_id`)
- Имеют свою цену продажи и закупки
- Имеют количество на складе
- Имеют параметры (цвет, размер) в `variant_params`
- Имеют статус доступности

**Пример структуры:**
```
Nike Dunk Low (родитель, productId=392655)
├── Nike Dunk Low 38 размер (variant, variantId=659423919)
├── Nike Dunk Low 38.5 размер (variant, variantId=659423921)
├── Nike Dunk Low 39 размер (variant, variantId=659423922)
└── ...
```

---

## 🎨 Что показывается в админке

### Колонка "Название"
- 📋 Артикул (vendor_code)
- 🏷 Бейдж Poizon (если импортирован)
- 🔗 Бейдж Variant (если это вариант)
- ⭐ Бейдж Limited (если лимитированная модель)

### Колонка "Цены"
- **Цена продажи** (зеленым)
- 💰 Закупочная цена (если есть)
- 📈 Наценка в % (если есть закупочная)
- ¥ Цена в юанях (если из Poizon)

### Колонка "Наличие"
- 📦 Количество на складе
- ⏳ "Под заказ" (если stock_count=0)
- 🚚 Время доставки (min-max дней)
- 🎨 Количество вариантов

### Колонка "Инфо"
- ✅ Статус синхронизации с Poizon
- ❤️ Количество в избранном
- 🌍 Страна происхождения

---

## 🔍 Детальная информация

### Характеристики (properties)

Автоматически парсятся из JSON:
- **Тип закрытия** → fastening (laces/velcro/zipper/slip_on)
- **Высота голенища** → height (low/mid/high)
- **Применимый сезон** → season (summer/winter/demi/all)
- **Основной цвет** → color_description
- **Релиз Свидание** → release_year (извлекается год)
- **Идентификатор стиля** → style_code
- **Материал верхней части** → upper_material
- **Материал подошвы** → sole_material

Также сохраняются все properties в JSON для полного доступа.

### Размерные сетки (sizes_data)

Сохраняется весь массив sizes[] из JSON:
```json
[
  {
    "name": "Европейский кодекс ЕС",
    "delimiter": ",",
    "value": "35.5,36,36.5,37.5,..."
  },
  {
    "name": "Кодекс красоты США",
    "delimiter": ",",
    "value": "3.5,4,4.5,5,..."
  },
  ...
]
```

### Параметры варианта (variant_params)

Для каждого варианта сохраняются его параметры:
```json
[
  {"key": "Цвет", "value": "Белое"},
  {"key": "Размер", "value": "38"}
]
```

---

## 📊 Примеры использования

### Получить все варианты товара
```php
$product = Product::findOne($id);
$variants = Product::find()
    ->where(['parent_product_id' => $product->id])
    ->all();
```

### Получить характеристики
```php
$properties = json_decode($product->properties, true);
foreach ($properties as $prop) {
    echo $prop['key'] . ': ' . $prop['value'] . "\n";
}
```

### Получить размерные сетки
```php
$sizes = json_decode($product->sizes_data, true);
foreach ($sizes as $sizeGrid) {
    echo $sizeGrid['name'] . ': ' . $sizeGrid['value'] . "\n";
}
```

### Получить параметры варианта
```php
$params = json_decode($variant->variant_params, true);
foreach ($params as $param) {
    echo $param['key'] . ': ' . $param['value'] . "\n";
}
// Выведет:
// Цвет: Белое
// Размер: 38
```

---

## ⚠️ Важные замечания

### 1. Варианты без stock
Варианты с `available: false` **НЕ импортируются**.

### 2. Обновление при повторном импорте
При повторном импорте:
- Товары **обновляются** по `poizon_id` + `poizon_variant_id`
- Изображения **пересоздаются** (удаляются старые, создаются новые)
- Характеристики **перезаписываются**

### 3. Производительность
- Каждый товар + варианты импортируются в отдельной транзакции
- Прогресс выводится каждые 10 товаров
- При ошибке товар пропускается, импорт продолжается

---

## 🐛 Troubleshooting

### Ошибка: "Failed to instantiate component..."
**Проблема:** Не применены миграции  
**Решение:**
```bash
php yii migrate
```

### Ошибка: "Class 'app\commands\PoizonImportJsonController' not found"
**Проблема:** Файл контроллера не найден  
**Решение:** Проверить что файл существует:
```bash
ls -la commands/PoizonImportJsonController.php
```

### Товары не показываются в админке
**Проблема:** Не обновлена модель Product  
**Решение:** Перезапустить PHP-FPM или сервер разработки

### JSON не загружается
**Проблема:** Недоступен URL  
**Решение:** Проверить доступность URL в браузере или использовать локальный файл:
```bash
# Скачать JSON
curl "https://..." -o /tmp/poizon_export.json

# Импортировать локальный файл
php yii poizon-import-json/run "/tmp/poizon_export.json"
```

---

## 📝 Changelog

### 04.11.2024
- ✅ Создана миграция для новых полей Product
- ✅ Создана миграция для poizon_id в Category/Brand
- ✅ Создан PoizonImportJsonController
- ✅ Улучшена админка товаров
- ✅ Добавлена поддержка вариантов (children)
- ✅ Добавлен парсинг всех характеристик из JSON

---

## 🎉 Готово!

Теперь ты можешь:
1. ✅ Импортировать товары из JSON файлов Poizon
2. ✅ Видеть все характеристики в админке
3. ✅ Работать с вариантами товаров
4. ✅ Видеть закупочные цены и наценку
5. ✅ Отслеживать количество на складе
6. ✅ Видеть время доставки

**Вопросы?** Проверь документацию или напиши в чат!
