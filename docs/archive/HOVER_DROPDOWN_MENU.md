# Документация: Меню с выпадающими списками при наведении

**Дата**: 04.11.2024  
**Версия**: 1.0  
**Статус**: ✅ Готово

---

## 🎯 Структура меню

### Полная структура:

```
📋 Заказы (наведение) ▼
  ├── Все заказы
  ├── Создать заказ (если admin/manager)
  └── Статистика

📦 Товары (наведение) ▼ (только для admin)
  ├── Все товары
  └── Дашборд Poizon

👥 Пользователи (только для admin)
⚙️ Настройки (только для admin)
👤 Профиль
🚪 Выход
```

---

## 🎨 Как работает

### 1. Наведение курсора на "Заказы"

**Что происходит:**
```
Пользователь наводит курсор → 
   ↓
Dropdown автоматически открывается →
   ↓
Показываются пункты:
   • Все заказы
   • Создать заказ (если права есть)
   • Статистика
   ↓
Стрелка вращается на 180°
Фон становится голубым
```

**Анимация:**
- Плавное появление (fadeIn 0.15s)
- Вращение стрелки (rotate 180deg)
- Подсветка фона

### 2. Наведение курсора на "Товары"

**Что происходит:**
```
Пользователь наводит курсор →
   ↓
Dropdown автоматически открывается →
   ↓
Показываются пункты:
   • Все товары
   • Дашборд Poizon
   ↓
Стрелка вращается на 180°
Фон становится голубым
```

**Анимация:**
- Плавное появление (fadeIn 0.15s)
- Вращение стрелки (rotate 180deg)
- Подсветка фона

---

## 💻 CSS реализация

### Открытие при наведении

```css
/* Открытие dropdown при наведении */
.navbar-nav .nav-item.dropdown:hover .dropdown-menu {
    display: block;
    animation: fadeIn 0.15s ease;
}
```

**Принцип работы:**
- При наведении на `.nav-item.dropdown`
- Внутренний `.dropdown-menu` получает `display: block`
- Запускается анимация fadeIn

### Подсветка при наведении

```css
.navbar-nav .nav-item.dropdown:hover .dropdown-toggle {
    background-color: rgba(74, 158, 255, 0.25);
    color: #fff !important;
}
```

**Эффект:**
- Фон становится голубым
- Текст становится белым
- Визуальная обратная связь

### Вращение стрелки

```css
.navbar-nav .nav-item.dropdown:hover .dropdown-toggle::after {
    transform: rotate(180deg);
}
```

**Эффект:**
- Стрелка плавно поворачивается вниз
- Показывает, что dropdown открыт

### Анимация появления

```css
@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(-5px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}
```

**Эффект:**
- Dropdown появляется сверху вниз
- Плавное увеличение прозрачности
- Длительность: 0.15 секунды

---

## 📋 PHP структура

### Меню "Заказы"

```php
// Заказы (dropdown)
$ordersItems = [
    ['label' => 'Все заказы', 'url' => ['/admin/orders']],
];

if ($user->isAdmin() || $user->isManager()) {
    $ordersItems[] = ['label' => 'Создать заказ', 'url' => ['/admin/create-order']];
}

$ordersItems[] = ['label' => 'Статистика', 'url' => ['/admin/statistics']];

$menuItems[] = [
    'label' => '<i class="bi bi-receipt me-2"></i>Заказы',
    'items' => $ordersItems,
];
```

**Особенности:**
- ✅ Динамический список пунктов
- ✅ "Создать заказ" только для admin/manager
- ✅ Иконка + текст в label

### Меню "Товары"

```php
// Товары (dropdown) - только для админов
if ($user->isAdmin()) {
    $menuItems[] = [
        'label' => '<i class="bi bi-box-seam me-2"></i>Товары',
        'items' => [
            ['label' => 'Все товары', 'url' => ['/admin/products']],
            ['label' => 'Дашборд Poizon', 'url' => ['/admin/poizon-import']],
        ],
    ];
}
```

**Особенности:**
- ✅ Только для администраторов
- ✅ Два пункта: товары и дашборд
- ✅ Иконка + текст в label

---

## 🎨 Визуальные эффекты

### При наведении на пункт верхнего меню

**Нормальное состояние:**
```
📋 Заказы
```

**При наведении:**
```
📋 Заказы ▲ (голубой фон, стрелка вверх)
  ├── Все заказы
  ├── Создать заказ
  └── Статистика
```

**Эффекты:**
1. Фон пункта: `rgba(74, 158, 255, 0.25)` (голубой)
2. Цвет текста: `#fff` (белый)
3. Стрелка: вращение на 180°
4. Dropdown: плавное появление

### При наведении на пункт dropdown

**Нормальное состояние:**
```
  Все заказы (серый текст)
```

**При наведении:**
```
│ Все заказы (белый текст, голубая линия слева)
```

**Эффекты:**
1. Фон: `rgba(74, 158, 255, 0.2)` (голубой)
2. Цвет: `#fff` (белый)
3. Левая граница: `#4a9eff` (голубая, 3px)
4. Сдвиг вправо: `padding-left: 1.2rem`

---

## 🔧 Технические детали

### Преимущества hover-открытия

✅ **Быстрее** - не нужно кликать  
✅ **Интуитивно** - как в классических меню  
✅ **Удобно** - сразу видно все пункты  
✅ **Современно** - стандарт для веб-приложений  

### Совместимость с кликом

**Меню работает двумя способами:**

1. **Hover** (наведение):
   ```css
   .nav-item.dropdown:hover .dropdown-menu {
       display: block;
   }
   ```

2. **Click** (клик) - Bootstrap стандарт:
   ```html
   <a class="dropdown-toggle" data-bs-toggle="dropdown">
   ```

**Результат:** Работает и при наведении, и при клике!

---

## 📱 Адаптивность

### Десктоп (>768px)

- ✅ Dropdown открывается при hover
- ✅ Анимация fadeIn
- ✅ Вращение стрелки
- ✅ Подсветка фона

### Мобильные (<768px)

- ✅ Dropdown открывается при клике (touch)
- ✅ Анимация сохраняется
- ✅ Burger menu для навигации
- ✅ Полноэкранное меню

---

## 🧪 Тестирование

### Проверка функциональности

1. **Наведение на "Заказы"**:
   - ✅ Dropdown открывается
   - ✅ Показываются все пункты
   - ✅ Стрелка вращается
   - ✅ Фон подсвечивается

2. **Наведение на "Товары"**:
   - ✅ Dropdown открывается (только для admin)
   - ✅ Показываются 2 пункта
   - ✅ Стрелка вращается
   - ✅ Фон подсвечивается

3. **Клик по пунктам**:
   - ✅ Переход на нужную страницу
   - ✅ Dropdown закрывается
   - ✅ URL меняется

4. **Hover на пунктах dropdown**:
   - ✅ Подсветка фона
   - ✅ Голубая линия слева
   - ✅ Сдвиг вправо
   - ✅ Cursor: pointer

---

## 🎯 Примеры использования

### Быстрый доступ к заказам

```
Пользователь хочет создать заказ:
1. Наводит курсор на "📋 Заказы"
2. Видит dropdown с пунктами
3. Кликает "Создать заказ"
4. Открывается форма создания
```

**Время:** ~1 секунда

### Быстрый доступ к дашборду Poizon

```
Админ хочет проверить импорт:
1. Наводит курсор на "📦 Товары"
2. Видит dropdown с пунктами
3. Кликает "Дашборд Poizon"
4. Открывается дашборд
```

**Время:** ~1 секунда

---

## 💡 Советы

### Для пользователей

1. **Просто наведите курсор** - не нужно кликать
2. **Dropdown откроется автоматически** - сразу видно все пункты
3. **Выберите нужный пункт** - один клик и готово

### Для разработчиков

1. **Используйте :hover** для быстрого доступа
2. **Добавляйте анимации** для плавности
3. **Тестируйте на разных устройствах** - touch и mouse

---

## 🎉 Итог

### Текущая структура меню:

```
📋 Заказы → (hover) → [Все заказы, Создать заказ, Статистика]
📦 Товары → (hover) → [Все товары, Дашборд Poizon]
👥 Пользователи
⚙️ Настройки
👤 Профиль
🚪 Выход
```

### Ключевые особенности:

✅ **Открытие при hover** - быстро и удобно  
✅ **Плавные анимации** - красиво и современно  
✅ **Четкая структура** - только нужные пункты  
✅ **Права доступа** - admin/manager контроль  
✅ **Отзывчивый дизайн** - работает везде  

---

**Автор**: Cascade AI Assistant  
**Дата**: 04.11.2024  
**Статус**: Production Ready ✅
