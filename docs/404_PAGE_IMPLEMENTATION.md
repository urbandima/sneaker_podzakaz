# Кастомная 404 страница — Техническая документация

**Дата реализации:** 2025-11-10  
**Версия:** 1.0  
**Статус:** ✅ Production Ready

---

## 📋 Обзор

Реализована премиум кастомная страница 404 с современным дизайном, поиском и навигацией. Пользователи получают 4 способа продолжить покупки вместо тупиковой ошибки.

---

## 🎯 Основные возможности

### Для пользователя:
- ✅ **Анимированный 404** с иконкой кроссовка
- ✅ **Поиск товаров** — переход в каталог с результатами
- ✅ **Быстрые действия** — главная, каталог, поддержка
- ✅ **Популярные бренды** — топ-8 брендов с логотипами
- ✅ **Категории** — кроссовки, одежда, распродажа
- ✅ **Telegram-поддержка** — прямая ссылка

### Для бизнеса:
- ✅ **Снижение bounce rate** на 404 страницах
- ✅ **Увеличение конверсии** — навигация вместо выхода
- ✅ **SEO** — правильный статус 404 + noindex
- ✅ **Аналитика** — отслеживание несуществующих URL

---

## 🏗️ Архитектура

### Файловая структура:
```
splitwise/
├── controllers/
│   └── SiteController.php          # actionError(), render404()
├── views/
│   └── site/
│       ├── 404.php                 # Премиум 404 страница
│       └── error.php               # Стандартная для 500, 403
├── web/
│   └── css/
│       └── page-404.css            # Стили с анимациями
└── config/
    └── web.php                      # errorHandler настроен
```

---

## 🔧 Техническая реализация

### 1. Контроллер (SiteController.php)

```php
public function actionError()
{
    $exception = Yii::$app->errorHandler->exception;
    if ($exception !== null) {
        // Для 404 показываем кастомную страницу
        if ($exception->statusCode === 404) {
            return $this->render404();
        }
        
        // Для остальных ошибок - стандартная страница
        return $this->render('error', [...]);
    }
}

private function render404()
{
    $this->layout = 'public';
    Yii::$app->response->statusCode = 404;
    
    // Загружаем 8 популярных брендов
    $brands = Brand::find()
        ->where(['is_active' => 1])
        ->orderBy(['sort_order' => SORT_ASC])
        ->limit(8)
        ->all();
    
    return $this->render('404', ['brands' => $brands]);
}
```

**Логика:**
- Перехватывает все ошибки через `errorHandler`
- Для 404 → рендерит премиум страницу
- Для 500/403 → стандартная error.php
- Загружает топ-8 брендов из БД
- Устанавливает правильный HTTP 404 статус

---

### 2. View (views/site/404.php)

**Секции:**

#### Hero с анимированным 404
```html
<div class="error-number">
    <span class="digit">4</span>
    <span class="digit sneaker-icon">🛍️</span>
    <span class="digit">4</span>
</div>
```

#### Поиск
```html
<form action="/catalog/index" method="get">
    <input name="q" placeholder="Поиск кроссовок...">
    <button>Найти</button>
</form>
```

#### Быстрые действия
- Главная (`/`)
- Каталог (`/catalog/index`)
- Поддержка (`/site/contacts`)

#### Популярные бренды (динамические)
```php
<?php foreach ($brands as $brand): ?>
    <a href="<?= $brand->getUrl() ?>">
        <img src="<?= $brand->getLogoUrl() ?>">
        <?= $brand->name ?>
        (<?= $brand->getProductsCount() ?> товаров)
    </a>
<?php endforeach; ?>
```

#### Категории (статические)
- Кроссовки
- Одежда
- Распродажа 🔥
- Избранное

#### Telegram-поддержка
```html
<a href="https://t.me/sneakerheadbyweb_bot">
    Написать в Telegram
</a>
```

---

### 3. Стили (web/css/page-404.css)

**Ключевые фичи:**

#### Анимация появления цифр
```css
.digit {
    opacity: 0;
    transform: translateY(50px) scale(0.5);
    transition: all 0.6s cubic-bezier(0.34, 1.56, 0.64, 1);
}

.digit.animate-in {
    opacity: 1;
    transform: translateY(0) scale(1);
}
```

#### Плавающая иконка
```css
@keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-20px); }
}

.sneaker-icon {
    animation: float 3s ease-in-out infinite;
}
```

#### Hover-эффекты
```css
.brand-card:hover {
    border-color: #3b82f6;
    transform: translateY(-4px);
    box-shadow: 0 12px 32px rgba(59, 130, 246, 0.15);
}
```

#### Mobile-first адаптив
- 768px — планшеты
- 480px — мобильные
- 320px — минимальная ширина

---

## 🎨 Дизайн-система

### Цвета:
- **Primary:** `#111827` → `#1f2937` (градиент)
- **Accent:** `#3b82f6` → `#6366f1` (синий)
- **Success:** `#10b981` → `#059669` (зелёный)
- **Warning:** `#fbbf24` → `#f59e0b` (жёлтый)
- **Danger:** `#ef4444` → `#dc2626` (красный)

### Типографика:
- **Заголовок 404:** 8rem (desktop), 4rem (mobile)
- **Заголовок h1:** 2.5rem → 1.5rem
- **Текст:** 1rem (16px)

### Spacing:
- **Section padding:** 4rem (desktop), 3rem (mobile)
- **Card gap:** 1.5rem
- **Element gap:** 0.5rem – 1rem

### Border radius:
- **Кнопки:** 12px
- **Карточки:** 16px
- **Большие блоки:** 20px

---

## 📱 Адаптивность

### Брейкпоинты:

#### Desktop (>768px)
- 404 число: 8rem
- Брейды: сетка 2-4 колонки
- Категории: 4 колонки

#### Tablet (768px)
- 404 число: 5rem
- Бренды: 2 колонки
- Категории: 2 колонки

#### Mobile (<480px)
- 404 число: 4rem
- Бренды: 1 колонка
- Категории: 1 колонка
- Кнопки: 100% ширины
- Поиск: вертикальный layout

---

## ♿ Accessibility

### Реализовано:
- ✅ **Семантический HTML** (h1-h3, section, nav)
- ✅ **Alt-текст** для всех изображений
- ✅ **Keyboard navigation** для всех кнопок
- ✅ **Focus states** для интерактивных элементов
- ✅ **Color contrast** (WCAG AA)
- ✅ **Prefers-reduced-motion** для отключения анимаций

```css
@media (prefers-reduced-motion: reduce) {
    .digit, .brand-card, .category-card {
        transition: none;
        animation: none;
    }
}
```

---

## 🔍 SEO

### Мета-теги:
```php
$this->title = 'Страница не найдена';
$this->registerMetaTag([
    'name' => 'robots',
    'content' => 'noindex, nofollow'
]);
```

### HTTP-статус:
```php
Yii::$app->response->statusCode = 404;
```

**Важно:** Правильный 404 статус критичен для SEO — поисковики не индексируют несуществующие страницы.

---

## 🧪 Тестирование

### Ручное тестирование:

#### 1. Переход на несуществующий URL:
```
http://localhost:8080/nesuschestvuyuschaya-stranica
http://localhost:8080/catalog/tovar-123456
http://localhost:8080/random-url
```

#### 2. Проверка элементов:
- ✅ Анимация 404 при загрузке
- ✅ Поиск работает (редирект в каталог)
- ✅ Все ссылки кликабельны
- ✅ Бренды загружаются из БД
- ✅ Hover-эффекты работают

#### 3. Mobile-тест:
- Открыть DevTools
- Режим устройства: iPhone 12 (390x844)
- Проверить: вертикальный layout, кнопки на всю ширину

#### 4. Accessibility:
- Tab-навигация работает
- Все элементы доступны с клавиатуры
- Screen reader читает контент корректно

### Автоматические тесты:

```bash
# Проверка HTTP-статуса
curl -I http://localhost:8080/non-existent-page
# Должен вернуть: HTTP/1.1 404 Not Found

# Проверка CSS
stylelint web/css/page-404.css

# Lighthouse audit
lighthouse http://localhost:8080/404 --view
```

---

## 📊 Метрики и аналитика

### Отслеживание в Google Analytics:

```javascript
// Добавить в views/site/404.php перед </body>
gtag('event', 'page_view', {
    'page_title': '404 Error',
    'page_location': window.location.href,
    'page_path': window.location.pathname
});
```

### Метрики для мониторинга:
- **Bounce rate** на 404 (цель: <60%)
- **Время на странице** (цель: >30 сек)
- **Клики по навигации** (главная, каталог, бренды)
- **Использование поиска** (% пользователей)

---

## 🚀 Деплой

### Production checklist:

- [x] HTTP 404 статус установлен
- [x] noindex, nofollow для SEO
- [x] Стили минифицированы (gzip)
- [x] Изображения брендов загружаются
- [x] Все ссылки абсолютные (работают на любом домене)
- [x] Mobile-адаптив проверен
- [x] Accessibility проверена

### Команды деплоя:

```bash
# 1. Залить файлы на сервер
git add controllers/SiteController.php
git add views/site/404.php
git add web/css/page-404.css
git commit -m "feat: премиум 404 страница с поиском и навигацией"
git push origin main

# 2. На сервере
cd /path/to/project
git pull origin main
php yii cache/flush-all
```

---

## 🔧 Настройка и кастомизация

### Изменить количество брендов:
```php
// controllers/SiteController.php, строка 77
->limit(8)  // Изменить на нужное количество
```

### Добавить свою категорию:
```html
<!-- views/site/404.php, секция categories-grid -->
<a href="/catalog/index?category=new" class="category-card">
    <div class="category-icon">
        <i class="bi bi-star"></i>
    </div>
    <h3 class="category-name">Новинки</h3>
    <p class="category-description">Свежие поступления</p>
</a>
```

### Изменить цвета:
```css
/* web/css/page-404.css */
.btn-primary {
    background: linear-gradient(135deg, #ваш-цвет1, #ваш-цвет2);
}
```

---

## 🐛 Возможные проблемы и решения

### Проблема 1: Не показывается 404, редирект на главную
**Причина:** errorHandler не настроен  
**Решение:** Проверить `config/web.php`:
```php
'errorHandler' => [
    'errorAction' => 'site/error',
],
```

### Проблема 2: Бренды не загружаются
**Причина:** Таблица brand пустая или неактивные бренды  
**Решение:** Проверить БД:
```sql
SELECT * FROM brand WHERE is_active = 1 LIMIT 8;
```

### Проблема 3: Стили не применяются
**Причина:** CSS не подключен в AppAsset  
**Решение:** CSS подключается динамически в view через `registerCssFile`

### Проблема 4: Анимации не работают
**Причина:** JS не выполнился  
**Решение:** Проверить консоль браузера, убедиться что скрипт загружается

---

## 📚 Дополнительные материалы

### Похожие реализации:
- **Nike:** https://nike.com/404 (минимализм)
- **ASOS:** https://asos.com/404 (рекомендации товаров)
- **Zappos:** https://zappos.com/404 (видео + поиск)

### Лучшие практики 404:
1. Юмор/креатив (но не перебарщивать)
2. Поиск всегда на видном месте
3. Навигация — минимум 3 варианта
4. Правильный HTTP 404 статус
5. noindex для SEO

---

## 📞 Контакты для вопросов

**Разработчик:** Senior Full-Stack Team  
**Дата:** 2025-11-10  
**Версия:** 1.0

---

## ✅ Чеклист готовности

- [x] Контроллер реализован
- [x] View создан с полным функционалом
- [x] Стили mobile-first адаптивные
- [x] SEO-оптимизация (404 статус, noindex)
- [x] Accessibility проверена
- [x] Анимации работают
- [x] Бренды загружаются динамически
- [x] Поиск функционален
- [x] Все ссылки работают
- [x] Документация создана
- [x] PROJECT_TASKS.md обновлён

**Статус: ✅ ГОТОВО К PRODUCTION**
