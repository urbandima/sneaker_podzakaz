# 🔍 НАЙДЕНА ПРИЧИНА: Товары не грузятся в каталоге

**Дата:** 2025-11-06  
**Статус:** ✅ Диагностика завершена

---

## ✅ Что работает корректно:

1. **Backend (PHP/Yii2):**
   - ✅ 46 активных товаров в БД
   - ✅ Контроллер `CatalogController::actionIndex()` загружает товары
   - ✅ Все связи работают: `brand`, `sizes`, `colors`
   - ✅ HTML генерируется правильно в `_products.php`
   - ✅ Изображения загружаются с CDN Poizon

2. **Данные:**
   ```
   ID: 5, Nike Dunk Low 355152-106
   ID: 6, Nike Данк FD8683-100
   ID: 7, Nike Kobe 8 HF9550-002
   ```

3. **HTML структура:**
   ```html
   <div class="products grid-5" id="products">
       <div class="product product-card modern-card">
           <a href="..." class="product-link">
               <div class="product-image-wrapper">
                   <img src="..." class="product-image primary">
               </div>
               <div class="info product-card-body">...</div>
           </a>
       </div>
   </div>
   ```

---

## ❌ ПРОБЛЕМА: Конфликт CSS стилей

### Описание:
В файле `/views/catalog/index.php` есть **два набора inline CSS стилей**, которые конфликтуют:

1. **Старые стили (строки 1122-1150):**
   ```css
   .product .img { ... }
   .product .info { ... }
   ```

2. **Новые стили (строки 1312-1350):**
   ```css
   .product-image-wrapper { ... }
   .product-card-body { ... }
   ```

### Конфликт:
- HTML в `_products.php` использует: `.product-image-wrapper` и `.product-card-body`
- Старые inline стили ожидают: `.product .img` и `.product .info`
- Результат: **товары рендерятся, но могут отображаться некорректно или скрываться**

---

## 🛠 РЕШЕНИЕ:

### Вариант 1: Удалить старые стили (РЕКОМЕНДУЕТСЯ)
Удалить строки **1122-1150** в `index.php`, которые содержат:
```css
.product .img{position:relative;padding-top:110%;background:#f9fafb}
.product .img img{position:absolute;top:0;left:0;width:100%;height:100%;object-fit:cover}
.product .info{padding:1.125rem}
```

### Вариант 2: Синхронизировать HTML с CSS
Обновить `_products.php`, чтобы использовать классы из старых стилей:
- Заменить `.product-image-wrapper` на `.img`
- Использовать только `.info` вместо `.info product-card-body`

### Вариант 3: Объединить стили
Добавить правила для обоих наборов классов в одном месте.

---

## 📋 План исправления:

1. ✅ **Диагностика завершена** - проблема найдена
2. ⏳ **Очистка inline стилей** - удалить дублирующие/конфликтующие стили
3. ⏳ **Проверка отображения** - открыть `/test-catalog-visual-v2.html`
4. ⏳ **Финальное тестирование** - проверить `/catalog/` в браузере

---

## 🧪 Тестовые файлы:

1. **`test-catalog-data.php`** - проверка backend данных ✅
2. **`web/test-catalog-visual-v2.html`** - визуальная проверка CSS
3. **Открыть в браузере:**
   - `http://localhost:8080/test-catalog-visual-v2.html`
   - `http://localhost:8080/catalog/`

---

## 📝 Дополнительная информация:

### Файлы, которые участвуют:
- `/views/catalog/index.php` - основной view (содержит inline стили)
- `/views/catalog/_products.php` - рендер товаров
- `/web/css/catalog-card.css` - основные стили карточек
- `/models/Product.php` - модель (работает корректно)
- `/controllers/CatalogController.php` - контроллер (работает корректно)

### Рекомендуемое действие:
**Удалить строки 1122-1150 из `index.php`**, так как они дублируют функционал `catalog-card.css` и создают конфликт.
