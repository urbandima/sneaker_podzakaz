# 🚀 Локальная разработка через SSH-туннель

## Описание решения

Этот проект настроен для работы с **удаленной БД через SSH-туннель**. Это означает:
- ✅ **Локально** вы работаете с реальными данными с продакшн сервера
- ✅ **На хостинге** приложение подключается к локальной БД сервера
- ✅ **Не нужно** устанавливать MySQL локально
- ✅ **Не нужно** дублировать базу данных
- ✅ **Один конфиг** для всех окружений (`config/db.php`)

## 📋 Быстрый старт

### Шаг 1: Запустить SSH-туннель

```bash
./start-ssh-tunnel.sh
```

**Важно:** Скрипт попросит ввести пароль SSH: `4R6xu){VWj`

После успешного запуска туннель будет работать в фоновом режиме.

### Шаг 2: Проверить подключение

```bash
./check-ssh-tunnel.sh        # Проверка туннеля
./test-db-connection.php     # Проверка подключения к БД
```

### Шаг 3: Запустить сайт

```bash
php yii serve
```

Откройте в браузере: http://localhost:8080

### Шаг 4: После работы остановить туннель

```bash
./stop-ssh-tunnel.sh
```

## 🔧 Как это работает

### Архитектура

```
[Ваш компьютер]                    [Удаленный сервер vh124.hoster.by]
     |                                        |
     | PHP приложение                         | MySQL сервер
     |    ↓                                   |    ↓
     | 127.0.0.1:3306 ←---SSH туннель---------→ localhost:3306
     |    ↓
     | Работает как будто БД локальная
```

### Конфигурация БД (`config/db.php`)

Файл **автоматически определяет окружение**:

- **Локально** (localhost, 127.0.0.1):
  - Подключается к `127.0.0.1:3306` (через SSH туннель)
  - Использует credentials продакшн БД
  
- **На хостинге** (sneaker-head.by):
  - Подключается к `localhost:3306` (локальная БД сервера)
  - Использует те же credentials

**Результат:** один конфиг работает везде!

## 📁 Структура файлов

```
.
├── start-ssh-tunnel.sh       # Запуск SSH туннеля
├── stop-ssh-tunnel.sh        # Остановка туннеля
├── check-ssh-tunnel.sh       # Проверка статуса
├── test-db-connection.php    # Тест подключения к БД
├── config/
│   └── db.php                # Универсальный конфиг БД (в Git)
└── LOCAL_DEVELOPMENT.md      # Эта инструкция
```

## 🔒 Безопасность

- ✅ Файл `config/db.php` **коммитится в Git**
- ✅ Содержит реальные credentials (они нужны для работы)
- ✅ Приватный репозиторий защищает данные
- ⚠️ **НЕ делайте репозиторий публичным!**

## 🐛 Решение проблем

### Ошибка "Connection refused"

```bash
# Проверьте туннель
./check-ssh-tunnel.sh

# Перезапустите туннель
./stop-ssh-tunnel.sh
./start-ssh-tunnel.sh
```

### Порт 3306 занят

Если у вас локально установлен MySQL:

```bash
# Остановите локальный MySQL
sudo brew services stop mysql

# Или измените порт в скриптах (например, на 3307)
```

### Туннель отключается

SSH туннель может разорваться при потере соединения. Просто перезапустите:

```bash
./start-ssh-tunnel.sh
```

### Access denied for user

Проверьте credentials в `config/db.php`:
- Username: `sneakerh_username_order_user`
- Password: `kefir1kefir`
- Database: `sneakerh_username_order_management`

## 📤 Деплой на хостинг

### Через Git (рекомендуется)

```bash
# 1. Коммит изменений
git add .
git commit -m "Ваше описание"
git push origin main

# 2. На сервере (через SSH или cPanel Terminal)
cd /home/sneakerh/public_html
git pull origin main
```

### Важно

- `config/db.php` **автоматически** переключится на продакшн режим
- Никаких дополнительных настроек не нужно
- Все работает "из коробки"

## 💡 Полезные команды

```bash
# Проверить статус туннеля
lsof -i :3306

# Убить процесс на порту 3306
lsof -ti :3306 | xargs kill -9

# Проверить SSH подключение
ssh -p 22 sneakerh@vh124.hoster.by

# Просмотр логов приложения
tail -f runtime/logs/app.log
```

## 🎯 Преимущества этого подхода

1. **Простота**: один конфиг для всех окружений
2. **Реальные данные**: работаете с актуальной БД
3. **Безопасность**: SSH шифрует трафик
4. **Скорость**: не нужно синхронизировать БД
5. **Git-friendly**: можно коммитить config/db.php

## 📞 Контакты сервера

- **SSH Host:** vh124.hoster.by
- **SSH Port:** 22
- **Username:** sneakerh
- **Password:** 4R6xu){VWj
- **Panel:** https://vh124.hoster.by:2083
- **Site Path:** /home/sneakerh/public_html

---

**Готово! Теперь вы можете разрабатывать локально и деплоить через Git.** 🎉
