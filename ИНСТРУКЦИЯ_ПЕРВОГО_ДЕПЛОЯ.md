# 🚀 Пошаговая инструкция первого деплоя на сервер

**Дата:** 05.11.2025  
**Сервер:** vh124.hoster.by  
**Пользователь:** sneakerh  
**Директория:** `/home/sneakerh/zakaz-test.sneaker-head.by`

---

## ❌ Обнаруженные проблемы

По результатам выполнения команд в терминале:

1. ❌ **Git репозиторий не инициализирован** - проект не склонирован
2. ❌ **PHP путь неверный** - ea-php80 не найден
3. ❌ **Директории проекта отсутствуют** - нечего обновлять

**Вывод:** Проект НЕ УСТАНОВЛЕН, нужна первоначальная установка.

---

## Шаг 1: Проверка текущего состояния

Выполните в SSH терминале:

```bash
# 1. Перейти в домашнюю директорию
cd ~

# 2. Проверить содержимое директории проекта
ls -la zakaz-test.sneaker-head.by/

# 3. Если директория пустая или почти пустая - удалите её
rm -rf zakaz-test.sneaker-head.by/
```

---

## Шаг 2: Найти правильную версию PHP

```bash
# Проверить доступные версии PHP в cPanel
ls -la /opt/cpanel/ea-php*/root/usr/bin/php

# ИЛИ короче:
which php

# ИЛИ проверить версию напрямую:
php -v
```

**Ожидаемый результат:** Что-то вроде:
```
PHP 8.0.30 (cli) ...
```

**Возможные пути к PHP:**
- `/usr/bin/php` (обычно дефолтный)
- `/opt/cpanel/ea-php74/root/usr/bin/php`
- `/opt/cpanel/ea-php80/root/usr/bin/php`
- `/opt/cpanel/ea-php81/root/usr/bin/php`
- `/opt/cpanel/ea-php82/root/usr/bin/php`

**Скопируйте правильный путь или просто используйте `php` если команда работает.**

---

## Шаг 3: Клонировать репозиторий из GitHub

### Вариант А: Если репозиторий публичный

```bash
cd /home/sneakerh/

git clone https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git zakaz-test.sneaker-head.by

cd zakaz-test.sneaker-head.by
```

### Вариант Б: Если репозиторий приватный (через HTTPS + Token)

```bash
cd /home/sneakerh/

# Замените:
# YOUR_USERNAME - ваш логин GitHub
# YOUR_TOKEN - Personal Access Token (создайте в GitHub Settings → Developer settings)
# YOUR_REPO_NAME - имя репозитория

git clone https://YOUR_USERNAME:YOUR_TOKEN@github.com/YOUR_USERNAME/YOUR_REPO_NAME.git zakaz-test.sneaker-head.by

cd zakaz-test.sneaker-head.by
```

**Как создать Personal Access Token:**
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token
3. Выберите права: `repo` (полный доступ к приватным репозиториям)
4. Скопируйте токен (он покажется только один раз!)

### Вариант В: Если репозиторий приватный (через SSH ключ)

```bash
# 1. Сгенерировать SSH ключ (если его нет)
ssh-keygen -t ed25519 -C "sneakerh@vh124.hoster.by"
# Нажимайте Enter на все вопросы

# 2. Скопировать публичный ключ
cat ~/.ssh/id_ed25519.pub

# 3. Добавить ключ в GitHub:
# GitHub → Settings → SSH and GPG keys → New SSH key → вставить ключ

# 4. Клонировать
cd /home/sneakerh/
git clone git@github.com:YOUR_USERNAME/YOUR_REPO_NAME.git zakaz-test.sneaker-head.by

cd zakaz-test.sneaker-head.by
```

---

## Шаг 4: Проверить что файлы загрузились

```bash
cd /home/sneakerh/zakaz-test.sneaker-head.by

# Проверить структуру проекта
ls -la

# Должны быть директории:
# - config/
# - controllers/
# - models/
# - views/
# - web/
# - migrations/
# - commands/
# И файлы:
# - composer.json
# - .env.example
# - .gitignore
```

**Если НЕ видите эти файлы - клонирование не удалось!**

---

## Шаг 5: Создать .env файл

```bash
cd /home/sneakerh/zakaz-test.sneaker-head.by

# Скопировать шаблон
cp .env.example .env

# Отредактировать (используйте nano, vim или через cPanel File Manager)
nano .env
```

**Минимальная конфигурация для .env:**

```env
# Environment
YII_ENV=prod
YII_DEBUG=false

# Security - СГЕНЕРИРУЙТЕ НОВЫЙ КЛЮЧ!
COOKIE_VALIDATION_KEY=ВАШ_УНИКАЛЬНЫЙ_КЛЮЧ_64_СИМВОЛА

# Database (данные из cPanel → MySQL Databases)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=sneakerh_zakaz_test
DB_USERNAME=sneakerh_zakaz_user
DB_PASSWORD=ВАШ_ПАРОЛЬ_ИЗ_CPANEL
DB_CHARSET=utf8mb4

# Database Performance
DB_SCHEMA_CACHE=true
DB_SCHEMA_CACHE_DURATION=3600

# Email
MAIL_USE_FILE_TRANSPORT=false
MAIL_FROM_EMAIL=noreply@sneaker-head.by
MAIL_FROM_NAME=СникерКультура

# SMTP (настройте для реальной отправки)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_ENCRYPTION=tls

# Admin
ADMIN_EMAIL=admin@sneaker-head.by
```

**Генерация COOKIE_VALIDATION_KEY:**

```bash
# Выполните на сервере:
openssl rand -hex 32

# ИЛИ:
php -r "echo bin2hex(random_bytes(32));"

# Скопируйте результат в .env
```

**Сохраните файл:**
- В nano: `Ctrl+O`, `Enter`, `Ctrl+X`
- В vim: `ESC`, `:wq`, `Enter`

---

## Шаг 6: Проверить/установить Composer

```bash
cd /home/sneakerh/zakaz-test.sneaker-head.by

# Проверить наличие composer.phar
ls -la composer.phar

# Если НЕТ - скачать:
curl -sS https://getcomposer.org/installer | php

# Проверить работу:
php composer.phar --version
```

**Ожидаемый результат:** `Composer version 2.x.x`

---

## Шаг 7: Установить зависимости

```bash
cd /home/sneakerh/zakaz-test.sneaker-head.by

# Установить зависимости Composer
php composer.phar install --no-dev --optimize-autoloader
```

**Время выполнения:** 1-3 минуты  
**Ожидаемый результат:** Создастся директория `vendor/` с зависимостями

**Если ошибка "memory limit":**
```bash
php -d memory_limit=-1 composer.phar install --no-dev --optimize-autoloader
```

---

## Шаг 8: Создать базу данных MySQL (если не создана)

### Через cPanel:

1. Войдите в cPanel
2. **MySQL® Databases**
3. **Create New Database:**
   - Database Name: `zakaz_test`
   - Полное имя будет: `sneakerh_zakaz_test`
4. **MySQL Users** → **Add New User:**
   - Username: `zakaz_user`
   - Password: (сгенерируйте сложный)
   - Полное имя будет: `sneakerh_zakaz_user`
5. **Add User To Database:**
   - User: `sneakerh_zakaz_user`
   - Database: `sneakerh_zakaz_test`
   - Privileges: **ALL PRIVILEGES**

**Скопируйте данные подключения в .env файл!**

---

## Шаг 9: Применить миграции базы данных

```bash
cd /home/sneakerh/zakaz-test.sneaker-head.by

# Применить все миграции
php yii migrate --interactive=0
```

**Ожидаемый результат:** 
```
Total 37 new migrations to be applied:
    m241023_181500_create_users_table
    m241023_181600_create_orders_table
    ...
*** applied m241023_181500_create_users_table (time: 0.123s)
...
Migrated up successfully.
```

**Если ошибка подключения к БД:**
- Проверьте данные в `.env` (DB_HOST, DB_NAME, DB_USERNAME, DB_PASSWORD)
- Проверьте что база создана в cPanel
- Проверьте что пользователь добавлен к базе с правами

---

## Шаг 10: Установить права доступа

```bash
cd /home/sneakerh/zakaz-test.sneaker-head.by

# Права на запись для веб-сервера
chmod 777 runtime/
chmod 777 web/uploads/
chmod 777 web/assets/

# Проверить что директории существуют
ls -la | grep -E 'runtime|web'
```

---

## Шаг 11: Настроить Document Root

**Document Root ДОЛЖЕН указывать на `/web`, НЕ на корень проекта!**

### Вариант А: Через симлинк (рекомендуется)

```bash
# Удалить старый public_html (если он есть)
cd /home/sneakerh/
rm -rf public_html

# Создать симлинк на web директорию проекта
ln -s /home/sneakerh/zakaz-test.sneaker-head.by/web public_html

# Проверить
ls -la public_html
```

### Вариант Б: Через cPanel

1. cPanel → **Domains**
2. Найдите домен `zakaz-test.sneaker-head.by`
3. **Manage** → **Document Root**
4. Измените на: `/home/sneakerh/zakaz-test.sneaker-head.by/web`
5. **Save**

---

## Шаг 12: Проверка работоспособности

### Через браузер:

1. Откройте: `http://zakaz-test.sneaker-head.by`
2. Должна открыться главная страница или страница логина
3. Попробуйте войти:
   - Логин: `admin`
   - Пароль: `admin123`

### Через curl (в SSH):

```bash
curl -I http://zakaz-test.sneaker-head.by
```

**Ожидаемый результат:**
```
HTTP/1.1 200 OK
...
```

**Если 404 Not Found:**
- Проверьте Document Root
- Проверьте `.htaccess` в `web/.htaccess`

**Если 500 Internal Server Error:**
```bash
# Посмотрите логи ошибок
tail -50 /home/sneakerh/zakaz-test.sneaker-head.by/runtime/logs/app.log

# ИЛИ логи Apache/nginx
tail -50 /home/sneakerh/logs/error_log
```

---

## Шаг 13: Изменить пароль администратора

**ОБЯЗАТЕЛЬНО!** После первого входа:

1. Войдите как `admin` / `admin123`
2. Перейдите в профиль (правый верхний угол)
3. Смените пароль на сложный

---

## 🎯 Скрипт автоматической установки

Создайте файл `/home/sneakerh/install-zakaz.sh`:

```bash
#!/bin/bash
set -e

REPO_URL="https://github.com/YOUR_USERNAME/YOUR_REPO.git"
PROJECT_DIR="/home/sneakerh/zakaz-test.sneaker-head.by"
PHP_BIN="php"  # Или полный путь: /opt/cpanel/ea-php82/root/usr/bin/php

echo "========================================="
echo "🚀 Установка проекта zakaz-test"
echo "========================================="

# 1. Проверка PHP
echo "Проверка PHP..."
$PHP_BIN -v || { echo "❌ PHP не найден!"; exit 1; }

# 2. Удаление старой директории (если есть)
if [ -d "$PROJECT_DIR" ]; then
    echo "⚠️  Директория существует, удаляем..."
    rm -rf "$PROJECT_DIR"
fi

# 3. Клонирование
echo "📥 Клонирование из GitHub..."
git clone "$REPO_URL" "$PROJECT_DIR" || { echo "❌ Git clone failed!"; exit 1; }

cd "$PROJECT_DIR"

# 4. Проверка .env.example
if [ ! -f ".env.example" ]; then
    echo "❌ .env.example не найден!"
    exit 1
fi

# 5. Создание .env
echo "📝 Создание .env файла..."
if [ ! -f ".env" ]; then
    cp .env.example .env
    echo "⚠️  ВНИМАНИЕ: Отредактируйте .env файл с вашими данными!"
    echo "   nano $PROJECT_DIR/.env"
    read -p "Нажмите Enter когда закончите редактирование .env..." dummy
fi

# 6. Composer
echo "📦 Установка composer.phar..."
if [ ! -f "composer.phar" ]; then
    curl -sS https://getcomposer.org/installer | $PHP_BIN
fi

echo "📦 Установка зависимостей..."
$PHP_BIN composer.phar install --no-dev --optimize-autoloader

# 7. Права доступа
echo "🔐 Установка прав..."
chmod 777 runtime/ web/uploads/ web/assets/

# 8. Миграции (спросить подтверждение)
read -p "Применить миграции БД? (y/n): " apply_migrations
if [ "$apply_migrations" = "y" ]; then
    echo "🗄️  Применение миграций..."
    $PHP_BIN yii migrate --interactive=0
fi

# 9. Симлинк (опционально)
read -p "Создать симлинк public_html → web? (y/n): " create_symlink
if [ "$create_symlink" = "y" ]; then
    cd /home/sneakerh/
    rm -rf public_html
    ln -s "$PROJECT_DIR/web" public_html
    echo "✅ Симлинк создан"
fi

echo "========================================="
echo "✅ Установка завершена!"
echo "========================================="
echo "Следующие шаги:"
echo "1. Проверьте .env файл: nano $PROJECT_DIR/.env"
echo "2. Откройте сайт в браузере: http://zakaz-test.sneaker-head.by"
echo "3. Войдите: admin / admin123"
echo "4. СРАЗУ смените пароль администратора!"
echo "========================================="
```

**Использование:**

```bash
chmod +x /home/sneakerh/install-zakaz.sh
/home/sneakerh/install-zakaz.sh
```

---

## 🔄 Скрипт обновления (после установки)

Создайте файл `/home/sneakerh/update-zakaz.sh`:

```bash
#!/bin/bash
set -e

PROJECT_DIR="/home/sneakerh/zakaz-test.sneaker-head.by"
PHP_BIN="php"
LOG_FILE="$HOME/deployment_$(date +%Y%m%d_%H%M%S).log"

echo "=========================================" | tee -a "$LOG_FILE"
echo "🚀 Обновление: $(date)" | tee -a "$LOG_FILE"
echo "=========================================" | tee -a "$LOG_FILE"

cd "$PROJECT_DIR" || exit 1

# Бекап БД
echo "📦 Бекап БД..." | tee -a "$LOG_FILE"
DB_NAME=$(grep DB_NAME .env | cut -d '=' -f2)
DB_USER=$(grep DB_USERNAME .env | cut -d '=' -f2)
DB_PASS=$(grep DB_PASSWORD .env | cut -d '=' -f2)

if [ -n "$DB_NAME" ]; then
    mkdir -p "$HOME/backups"
    mysqldump -u "$DB_USER" -p"$DB_PASS" "$DB_NAME" > "$HOME/backups/db_$(date +%Y%m%d_%H%M%S).sql" 2>&1 | tee -a "$LOG_FILE"
fi

# Git обновление
echo "📥 Git update..." | tee -a "$LOG_FILE"
git fetch origin main 2>&1 | tee -a "$LOG_FILE"
git reset --hard origin/main 2>&1 | tee -a "$LOG_FILE"

# Composer
echo "📦 Composer..." | tee -a "$LOG_FILE"
$PHP_BIN composer.phar install --no-dev --optimize-autoloader 2>&1 | tee -a "$LOG_FILE"

# Миграции
echo "🗄️  Миграции..." | tee -a "$LOG_FILE"
$PHP_BIN yii migrate --interactive=0 2>&1 | tee -a "$LOG_FILE"

# Очистка кеша
echo "🧹 Очистка кеша..." | tee -a "$LOG_FILE"
rm -rf runtime/cache/* web/assets/* 2>&1 | tee -a "$LOG_FILE"

# Права
chmod 777 runtime/ web/uploads/ web/assets/ 2>&1 | tee -a "$LOG_FILE"

echo "=========================================" | tee -a "$LOG_FILE"
echo "✅ Обновление завершено!" | tee -a "$LOG_FILE"
echo "📋 Лог: $LOG_FILE" | tee -a "$LOG_FILE"
echo "=========================================" | tee -a "$LOG_FILE"
```

**Использование:**

```bash
chmod +x /home/sneakerh/update-zakaz.sh
/home/sneakerh/update-zakaz.sh
```

---

## 📋 Итоговый чеклист

### Перед установкой:
- [ ] Создан репозиторий на GitHub
- [ ] Код залит в GitHub (git push)
- [ ] Создана MySQL база в cPanel
- [ ] Создан MySQL пользователь с правами

### Установка:
- [ ] Проект склонирован из GitHub
- [ ] `.env` файл создан и заполнен
- [ ] `composer.phar` установлен
- [ ] Зависимости установлены (`composer install`)
- [ ] Миграции применены (`php yii migrate`)
- [ ] Права 777 на runtime/uploads/assets
- [ ] Document Root → `/web`

### Проверка:
- [ ] Сайт открывается в браузере
- [ ] Логин admin/admin123 работает
- [ ] Пароль администратора изменён
- [ ] Email отправляются (проверить в админке)

### После установки:
- [ ] Создан скрипт обновления `update-zakaz.sh`
- [ ] Настроен SSL сертификат (Let's Encrypt)
- [ ] Настроен бекап БД (cron)

---

## ❓ Частые проблемы

### "Git clone" требует логин/пароль
**Решение:** Используйте Personal Access Token или SSH ключ (см. Шаг 3)

### "PHP version not found"
**Решение:** Найдите правильный путь через `which php` или `ls /opt/cpanel/ea-php*/root/usr/bin/php`

### "Class not found" после composer install
**Решение:**
```bash
php composer.phar dump-autoload --optimize
```

### "Database connection failed"
**Решение:** Проверьте данные в `.env` и права пользователя в MySQL

### 404 на всех страницах кроме главной
**Решение:** Проверьте `.htaccess` и `mod_rewrite`:
```bash
# В web/.htaccess должно быть:
RewriteEngine on
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . index.php
```

---

**✅ Следуйте инструкциям пошагово и установка пройдёт успешно!**
