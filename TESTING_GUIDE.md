# Руководство по тестированию фильтров

## Быстрый тест в браузере

### 1. Откройте страницу каталога
```
http://localhost/catalog
```

### 2. Откройте консоль разработчика
- **Windows/Linux:** `F12` или `Ctrl+Shift+I`
- **Mac:** `Cmd+Option+I`

### 3. Проверьте фильтр "Пол"

#### Шаг 1: Откройте фильтры
- Нажмите на кнопку "Фильтры" (на мобильной версии)
- Или прокрутите к боковой панели фильтров (на десктопе)

#### Шаг 2: Найдите фильтр "Пол"
- Фильтр "Пол" должен быть в основной секции (не в расширенных фильтрах)
- Проверьте, что отображаются счетчики:
  - ✅ Мужской (12)
  - ✅ Женский (8)
  - ✅ Унисекс (0)

#### Шаг 3: Выберите "Мужской"
- Отметьте чекбокс "Мужской"
- В консоли должны появиться логи:
  ```
  🔄 Обновление счетчиков фильтров: {...}
  📊 Обновление характеристики: Пол (char_3) {...}
    ✓ Мужской: 12
    ✓ Женский: 8
    ✓ Унисекс: 0
  ```

#### Шаг 4: Проверьте результаты
- Должно отобразиться 12 товаров
- Счетчик в заголовке: "Каталог товаров (12)"
- Все товары должны быть мужскими

### 4. Комбинированный тест

#### Тест 1: Пол + Бренд
1. Выберите "Мужской"
2. Выберите бренд "Nike"
3. Проверьте:
   - Счетчики других брендов обновились
   - Счетчик "Женский" обновился (показывает товары Nike женские)
   - Товары соответствуют обоим фильтрам

#### Тест 2: Пол + Цена
1. Выберите "Женский"
2. Установите цену "от 100 до 500"
3. Проверьте:
   - Отображаются только женские товары в указанном ценовом диапазоне
   - Счетчики брендов обновились с учетом обоих фильтров

### 5. Проверка динамического обновления

#### Что должно происходить:
- ✅ При выборе фильтра счетчики других фильтров пересчитываются
- ✅ Опции с 0 товаров становятся disabled (серыми)
- ✅ Активные фильтры отображаются в виде тегов над каталогом
- ✅ Пагинация обновляется корректно
- ✅ URL обновляется без перезагрузки страницы

## Ожидаемые логи в консоли

### При загрузке страницы:
```
✅ Все CSS файлы каталога загружены корректно
✅ Новые стили применены (80% ширина)
```

### При выборе фильтра:
```
🔄 Обновление счетчиков фильтров: {brands: Array(10), categories: Array(5), characteristics: Array(6), ...}
📊 Обновление характеристики: Пол (char_3) {id: 3, key: "gender", name: "Пол", type: "select", values: Array(3)}
  ✓ Мужской: 12
  ✓ Женский: 8
  ✓ Унисекс: 0
✅ Ответ от сервера: {success: true, html: "...", filters: {...}, pagination: {...}}
```

## Возможные проблемы и решения

### Проблема 1: Счетчики не обновляются
**Решение:**
1. Очистите кэш браузера (`Ctrl+Shift+R` или `Cmd+Shift+R`)
2. Очистите кэш приложения: `php yii cache/flush-all`
3. Проверьте консоль на ошибки JavaScript

### Проблема 2: Фильтр "Пол" не отображается
**Решение:**
1. Проверьте, что характеристика активна: `is_active = 1`
2. Проверьте, что включена в фильтры: `is_filter = 1`
3. Запустите тест: `php test-filter-gender.php`

### Проблема 3: Счетчики показывают 0
**Решение:**
1. Проверьте, что товары имеют характеристику "Пол"
2. Проверьте таблицу `product_characteristic_value`:
   ```sql
   SELECT COUNT(*) FROM product_characteristic_value WHERE characteristic_id = 3;
   ```
3. Добавьте характеристики товарам через админ-панель

### Проблема 4: Ошибка в консоли
**Решение:**
1. Откройте вкладку "Network" в DevTools
2. Найдите запрос `/catalog/filter`
3. Проверьте Response — должен быть JSON с `success: true`
4. Если ошибка 500 — проверьте логи: `runtime/logs/app.log`

## Автоматический тест

Запустите тестовый скрипт для проверки backend:
```bash
php test-filter-gender.php
```

Ожидаемый результат:
```
✅ Найдена: ID=3, Name=Пол
✅ Найдено 20 связей товар-пол
✅ Характеристики найдены: 6 шт.
✅ Характеристика 'Пол' в фильтрах
   - Мужской: 12 товаров
   - Женский: 8 товаров
   - Унисекс: 0 товаров
✅ Найдено товаров: 12
```

## Production Checklist

Перед развертыванием на production:

- [ ] Отключить логирование в `catalog.js` (закомментировать `console.log()`)
- [ ] Проверить индексы БД на таблицах:
  - `product_characteristic_value.product_id`
  - `product_characteristic_value.characteristic_id`
  - `product_characteristic_value.characteristic_value_id`
- [ ] Настроить кэширование (TTL для фильтров)
- [ ] Провести нагрузочное тестирование
- [ ] Проверить работу на мобильных устройствах
- [ ] Убедиться, что все товары имеют характеристики

## Контакты поддержки

При возникновении проблем:
1. Проверьте логи в консоли браузера
2. Проверьте логи Yii2: `runtime/logs/app.log`
3. Используйте Yii2 Debug Panel для анализа SQL запросов
4. Обратитесь к документации: `FILTER_FIXES.md`
