# 🔍 Руководство по отладке импорта Poizon

**Дата**: 04.11.2024, 23:32  
**Статус**: Готово к использованию

---

## 🎯 Проблема решена!

Добавлено **детальное логирование ошибок** при импорте товаров из JSON.

---

## ✅ Что теперь доступно

### 1. **Автоматический лог-файл**

При каждом импорте создается детальный лог:
```
runtime/logs/poizon-import-2024-11-04_23-32-15.log
```

**Что записывается**:
- Все ошибки с полным stack trace
- Данные товара, на котором произошла ошибка
- Индекс товара в JSON файле
- Product ID и название

---

### 2. **Verbose режим для детального вывода**

```bash
# Обычный режим (только ошибки)
php yii poizon-import-json/run "URL"

# Детальный режим (каждый товар + подробности ошибок)
php yii poizon-import-json/run "URL" --verbose=1
```

**В verbose режиме видно**:
```
🔄 [1/100] Импортирую: Nike Air Max (ID: 123456)...
   ✅ Успешно

🔄 [2/100] Импортирую: Adidas Yeezy (ID: 123457)...
   ✅ Успешно

❌ ОШИБКА [3/100]: New Balance 990 (ID: 123458)
   Ошибка сохранения товара: slug: Slug уже существует
   Trace: #0 commands/PoizonImportJsonController.php(374)...
```

---

### 3. **Детальные сообщения об ошибках**

**БЫЛО** (неинформативно):
```
❌ Ошибка: Ошибка сохранения товара: {"slug":["Slug уже существует"],"brand_id":["Бренд не найден"]}
```

**СТАЛО** (понятно):
```
❌ ОШИБКА [15/100]: Nike Air Jordan 1 (ID: 234567)
   Ошибка сохранения товара: slug: Slug уже существует; brand_id: Бренд не найден
```

---

### 4. **Отчет об ошибках в конце импорта**

После импорта выводится полный отчет:

```
╔══════════════════════════════════════════════════════╗
║  ⚠️  ДЕТАЛИ ОШИБОК                                  ║
╚══════════════════════════════════════════════════════╝

[1] Nike Air Jordan 1 (ID: 234567)
    Ошибка сохранения товара: slug: Slug уже существует

[2] Adidas Ultraboost (ID: 234568)
    Ошибка сохранения варианта: size: Размер обязателен

[3] New Balance 990v5 (ID: 234569)
    Бренд не найден: New Balance

💾 Полный лог ошибок: runtime/logs/poizon-import-2024-11-04_23-32-15.log
```

---

## 📖 Как использовать

### Простой импорт (с ошибками в консоли)

```bash
php yii poizon-import-json/run "https://your-storage.com/export.json"
```

**Что увидите**:
- Прогресс импорта каждые 10 товаров
- Ошибки сразу в консоли
- Итоговую статистику
- Путь к файлу лога

---

### Детальный импорт (с полной отладкой)

```bash
php yii poizon-import-json/run "https://your-storage.com/export.json" --verbose=1
```

**Что увидите**:
- Каждый импортируемый товар
- Статус каждой операции
- Полный stack trace для ошибок
- Все логи дублируются в файл

---

### Анализ лога после импорта

```bash
# Просмотреть последний лог
cat runtime/logs/poizon-import-*.log | tail -n 100

# Найти все ошибки в логе
grep "ОШИБКА" runtime/logs/poizon-import-*.log

# Найти лог конкретного товара
grep "Product ID: 123456" runtime/logs/poizon-import-*.log
```

---

## 🔍 Типичные ошибки и решения

### Ошибка #1: "slug: Slug уже существует"

**Причина**: Товар с таким названием уже импортирован

**Решение**:
```php
// Товар пропускается автоматически при повторном импорте
// Если нужно обновить - удалите дубликат вручную или измените логику
```

---

### Ошибка #2: "brand_id: Бренд не найден"

**Причина**: Бренд не был импортирован или название не совпадает

**Проверка**:
```sql
SELECT * FROM brand WHERE name = 'Nike';
```

**Решение**:
1. Проверить, что бренды импортируются из JSON (секция `brands`)
2. Проверить правильность поля `vendor` или `vendorId` в товаре
3. Создать бренд вручную через админку

---

### Ошибка #3: "category_id: Категория не найдена"

**Причина**: Категория не была импортирована или ID не совпадает

**Проверка**:
```sql
SELECT * FROM category WHERE poizon_id = 123;
```

**Решение**:
1. Проверить, что категории импортируются из JSON (секция `categories`)
2. Проверить правильность поля `categoryId` в товаре
3. Создать категорию вручную через админку

---

### Ошибка #4: "price: Цена обязательна"

**Причина**: В JSON отсутствует поле `price`

**Проверка лога**:
```json
"Product Data": {
  "title": "Nike Air Max",
  "price": null  ← Проблема здесь
}
```

**Решение**:
- Проверить структуру JSON файла
- Убедиться, что поле `price` заполнено
- Добавить дефолтную цену в коде

---

### Ошибка #5: "Ошибка сохранения варианта"

**Причина**: Ошибка в данных варианта (children)

**Проверка лога**:
```
Product Data:
  "children": [
    {
      "variantId": null,  ← Проблема
      "available": true
    }
  ]
```

**Решение**:
- Проверить структуру `children` в JSON
- Убедиться, что все обязательные поля заполнены

---

## 📝 Структура лог-файла

```
╔══════════════════════════════════════════════════════╗
║  🚀 ИМПОРТ ТОВАРОВ ИЗ POIZON JSON                  ║
╚══════════════════════════════════════════════════════╝

📡 Загружаю JSON из: https://...

═══════════════════════════════════════════════════
ОШИБКА #1: Nike Air Jordan 1
═══════════════════════════════════════════════════
Product ID: 234567
Index: 15/100
Error: Ошибка сохранения товара: slug: Slug уже существует

Stack Trace:
#0 commands/PoizonImportJsonController.php(374): Product->save()
#1 commands/PoizonImportJsonController.php(135): importProduct()
...

Product Data:
{
  "productId": 234567,
  "title": "Nike Air Jordan 1 Retro High OG",
  "vendor": "Nike",
  "price": 150,
  "images": [...]
}

🎉 Импорт завершен!
Лог сохранен: runtime/logs/poizon-import-2024-11-04_23-32-15.log
```

---

## 🎯 Быстрая диагностика проблем

### Шаг 1: Запустить импорт с verbose

```bash
php yii poizon-import-json/run "URL" --verbose=1 2>&1 | tee import.log
```

### Шаг 2: Найти первую ошибку

```bash
grep -A 5 "ОШИБКА" import.log | head -n 20
```

### Шаг 3: Открыть детальный лог

```bash
cat runtime/logs/poizon-import-*.log | less
```

### Шаг 4: Найти проблемный товар

```bash
# В логе будет Product ID и полные данные
grep "Product Data" runtime/logs/poizon-import-*.log -A 20
```

---

## 💡 Советы по отладке

### Совет #1: Используйте verbose для первого импорта

При первом импорте **всегда** используйте `--verbose=1`, чтобы видеть, что происходит.

### Совет #2: Сохраняйте логи

```bash
# Сохранить полный вывод в файл
php yii poizon-import-json/run "URL" --verbose=1 > debug.log 2>&1
```

### Совет #3: Тестируйте на малой выборке

Отредактируйте JSON, оставив только 5-10 товаров для теста:

```json
{
  "categories": [...],
  "brands": [...],
  "products": [
    {...},  // Только 5 товаров
    {...},
    {...},
    {...},
    {...}
  ]
}
```

### Совет #4: Проверяйте структуру JSON

```bash
# Проверить валидность JSON
cat export.json | jq .

# Посмотреть первый товар
cat export.json | jq '.products[0]'

# Посмотреть все поля первого товара
cat export.json | jq '.products[0] | keys'
```

---

## 🚀 Примеры команд

```bash
# 1. Простой импорт
php yii poizon-import-json/run "https://storage.com/export.json"

# 2. Детальный импорт
php yii poizon-import-json/run "https://storage.com/export.json" --verbose=1

# 3. Импорт с сохранением лога
php yii poizon-import-json/run "https://storage.com/export.json" --verbose=1 2>&1 | tee import.log

# 4. Импорт локального файла
php yii poizon-import-json/run "/tmp/export.json"

# 5. Импорт с перенаправлением всех ошибок
php yii poizon-import-json/run "URL" 2> errors.log
```

---

## 📊 Что записывается в лог

| Тип данных | Описание | Где найти |
|------------|----------|-----------|
| **Заголовок** | Начало импорта, URL файла | В начале лога |
| **Ошибки** | Полный stack trace + данные товара | Секция "ОШИБКА #N" |
| **Product ID** | ID товара в Poizon | Поле "Product ID" |
| **Индекс** | Номер товара в JSON | Поле "Index: N/Total" |
| **Сообщение** | Текст ошибки | Поле "Error:" |
| **Trace** | Stack trace | Секция "Stack Trace" |
| **Данные** | Полный JSON товара | Секция "Product Data" |
| **Итог** | Финальная статистика | В конце лога |

---

## ✅ Чеклист отладки

- [ ] Запустил импорт с `--verbose=1`
- [ ] Проверил консольный вывод на ошибки
- [ ] Открыл лог-файл для деталей
- [ ] Нашел первую ошибку
- [ ] Посмотрел данные проблемного товара
- [ ] Проверил БД (бренды, категории)
- [ ] Исправил проблему
- [ ] Запустил повторный импорт

---

## 🎉 Готово!

Теперь вы можете:
1. ✅ Видеть все ошибки импорта в реальном времени
2. ✅ Получать детальные логи для отладки
3. ✅ Анализировать проблемные товары
4. ✅ Быстро находить и исправлять ошибки

**Файл лога** содержит всю необходимую информацию для диагностики проблем импорта!
